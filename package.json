{
  "name": "open-world-arpg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tooling manifest ONLY. The shipped game imports nothing but relative paths and is served as static files with no build step. Node 22's built-in test runner means this has zero dependencies.",
  "scripts": {
    "test": "node --test tools/test/",
    "lint": "node tools/lint-layers.js",
    "validate": "node tools/validate-data.js",
    "check": "node tools/lint-layers.js && node tools/validate-data.js && node --test tools/test/"
  }
}
