{
  "decision": "NO_CHANGE",
  "candidates": [],
  "reusable_findings": [
    "When independently evaluated behavior depends on runtime configuration, bind a canonical public provider/model/runner manifest digest into the exact evaluation subject rather than relying on source identity alone.",
    "A producer-declared runtime manifest must remain explicitly unverified until an independent evaluator confirms and signs what was actually executed."
  ],
  "reason": "The validated improvement is implemented in code and tests; this persistence step does not justify a semantic prompt Candidate."
}
