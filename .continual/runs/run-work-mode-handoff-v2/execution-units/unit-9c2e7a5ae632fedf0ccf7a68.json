{
  "component": "execute",
  "evidence_needed": [
    "a selector request artifact exposes situations and child summaries but contains no required paths, forbidden skill IDs, eager baselines, hidden labels, or scorer decision",
    "a separate sealed scorer artifact and both artifacts bind the same frozen protocol digest without making hidden fields selector-visible",
    "submission qualification binds executor identity, selector request digest, protocol digest, source request identity, and a declaration that the selector had not seen sealed labels before producing the plan",
    "the current execution context and any executor recorded as label-exposed are rejected before replay or observation persistence",
    "duplicate, stale, mismatched, unqualified, post-relabeled, or partially covered submissions fail closed and create no observation receipt",
    "qualified submissions create deterministic append-only receipts that cross-bind the plan, two replays, scorer artifact, and resulting observation digest",
    "zero or incomplete qualified receipts retain INSUFFICIENT_EVIDENCE and cannot authorize scoped or global routing",
    "focused tests, protected Context Kernel and routing regressions, journal verification, exact-head CI, expected-head merge, and exact main readback"
  ],
  "expected_outputs": [
    "a public selector packet envelope and a separately sealed scorer envelope with canonical digests",
    "a qualification and contamination registry contract for selector executions",
    "append-only protocol-bound observation receipts and a deterministic aggregation path",
    "tests covering successful qualified ingestion and every contamination, replay, identity, label-leak, mutation, duplicate, and atomicity failure boundary",
    "updated Context Kernel architecture and durable native O continuation with explicit zero-observation negative evidence until a genuinely fresh selector participates",
    "an isolated exact-head PR and merge/readback only if every validation gate passes"
  ],
  "failure_conditions": [
    "hidden paths, forbidden labels, eager baselines, or scorer decisions appear in selector-visible bytes",
    "qualification can be self-asserted after label exposure or replayed under another executor/request/protocol",
    "measurement accepts missing, duplicate, stale, mismatched, relabeled, or non-append-only receipts",
    "a failed submission mutates observations, decisions, or an existing receipt",
    "the implementation fabricates a held-out observation from this contaminated context or reports internal CI as behavioral success",
    "lease, inbox, exact-head, conflict, or readback safety gates fail"
  ],
  "goal": "Implement a fail-closed held-out routing observation rendezvous that keeps selector-visible packets separate from sealed scorer labels, binds every submitted plan to the frozen protocol and executor identity, rejects contaminated or same-context submissions, and emits append-only qualified observation receipts before any routing verdict can be measured.",
  "inputs": [
    "agi/CONTEXT_ROUTING_EXPERIMENT.json frozen HARNESS_READY protocol with zero observations",
    "src/agi/context_routing_experiment.py public selection packet, plan validation, replay, and mechanical scorer",
    "the prior native contamination result showing this current context has seen the hidden labels",
    "run-work-mode-handoff-v2 immutable invocation journal and DecisionContextManifest source clocks",
    "generation-6 single-writer publication and exact-head CI contract"
  ],
  "parent_continuation": "After Execute, Task Evaluate the exact label-separation, qualification, contamination-rejection, receipt atomicity, test, CI, merge, and readback evidence. Consolidate and Learn, then return to Root. A later genuinely fresh selector may use the public request, but this current label-exposed context must never supply an admissible plan; without such participation retain INSUFFICIENT_EVIDENCE and select another independent unit rather than stopping.",
  "rollback": "Keep the frozen HARNESS_READY experiment, zero observations, INSUFFICIENT_EVIDENCE verdict, and no activation. Revert only the rendezvous layer if it leaks labels, admits contaminated identity, weakens frozen bindings, or cannot preserve append-only atomicity.",
  "scope": "context-kernel/heldout-routing-observation-rendezvous-v1",
  "success_conditions": [
    "no selector can obtain hidden scorer labels through the selector-visible artifact",
    "no label-exposed executor, including this current context, can create an admissible observation",
    "a synthetically qualified test executor can produce a fully cross-bound receipt and deterministic mechanical score without weakening the frozen thresholds",
    "unqualified or incomplete evidence leaves the checked-in experiment at INSUFFICIENT_EVIDENCE with no activation",
    "all protected tests, exact-head CI, conflict proof, merge, and readback succeed while generation 6 remains the sole main integrator"
  ],
  "unit_id": "unit-9c2e7a5ae632fedf0ccf7a68"
}
