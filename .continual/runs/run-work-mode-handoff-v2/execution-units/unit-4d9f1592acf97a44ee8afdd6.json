{
  "component": "execute",
  "evidence_needed": [
    "a canonical DecisionContextManifest with source kind, authoritative locator, revision or digest, observation time, mandatory/optional role, inclusion or exclusion reason, dependency binding, and a stable manifest digest",
    "Root request freezing that always binds the effective user-input inbox, current single-writer lease identity/fence, current strategy, and native run snapshot without copying every raw external payload into the prompt",
    "fail-closed tests for a missing mandatory source, revision/digest disagreement, and malformed lease/inbox control metadata",
    "replay tests showing one frozen pending request is byte/digest stable after a source advances and the next safe Root receives a different manifest/source clock",
    "behavioral tests showing an outside-only user constraint cannot silently influence O until represented by an ingested O source reference, and once ingested it is mandatory in the Root decision manifest",
    "focused regression, compileall, JSON validation, work-verify, exact diff review, then isolated exact-head CI before any main merge"
  ],
  "expected_outputs": [
    "a small Context Kernel module and Root-only Work bridge integration with no global raw-context copy",
    "an immutable request field containing the manifest plus digest and explicit included/excluded source decisions",
    "tests that distinguish source authority, O-owned decision context, replay stability, freshness, and next-boundary invalidation",
    "a durable architecture decision: O is the decision-control authority; GitHub, CI, providers, and other systems remain authoritative raw sources accessed through observation receipts; the outer session is an executor rather than an untracked context authority",
    "negative evidence and rollback guidance if the manifest cannot close the omission/staleness gap without breaking replay or single-writer safety"
  ],
  "goal": "Implement the first O-owned Context Kernel vertical slice: freeze a deterministic, provenance-bound DecisionContextManifest into each newly created Root Work request, fail closed when mandatory control sources are missing or internally inconsistent, and prove that an already-frozen pending request remains replay-stable while the next Root observes changed source revisions.",
  "inputs": [
    "inbox revision 15 and blob 6d360fba463dee47ac777a378dab2ccfe5ad05c3, which directs O-centered context management and explicitly asks for comparison with alternatives",
    "WORK_EXECUTION_STATE blob 1bbc4bcefddec9c6f6b35ff129af32bc26acf7f8 with generation 5, the current fence, acknowledged revision 14, and a fresh same-owner heartbeat",
    "current Root request invoke-344455ae7a59ec86e4fa5d20, whose payload contains run state but no inbox revision, lease/source clock, inclusion/exclusion ledger, freshness, or invalidation dependencies",
    "src/continual/work_session.py and the immutable Work request/response journal",
    "the checked-in recursive context-routing experiment, retained as optional branch-selection machinery rather than a substitute for mandatory control context"
  ],
  "parent_continuation": "After Execute, run Task Evaluate on omission, staleness, replay, and fail-closed behavior; consolidate and learn. If the Root-only slice passes, publish it through an isolated exact-head PR and then expand manifests to Execute/effect authorization and inbox-to-O event ingestion rather than declaring the context problem solved.",
  "scope": "context-kernel/root-decision-manifest-v1",
  "unit_id": "unit-4d9f1592acf97a44ee8afdd6"
}
