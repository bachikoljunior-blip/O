{
  "candidates": [],
  "decision": "NO_CHANGE",
  "observations": [
    "Strict type checks are necessary because JSON booleans are Python integers and could otherwise pass integer validation.",
    "Comparing the validated payload back to the source statement prevents silent lowercase/type normalization from changing the bytes an evaluator thought it supplied.",
    "Testing an already-existing output catches destructive failure behavior that an absent-output-only test misses."
  ]
}
