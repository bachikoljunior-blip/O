{
  "decision": "RETAIN_BOUNDARY",
  "observation": "A stale unmeasured premise can be repaired from immutable receipts without new trials when source identities, invocation bindings, replay outputs, and aggregate metrics all verify; the resulting negative must remain exact-scope and equivalent controls should be reused."
}
