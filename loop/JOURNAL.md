# 日誌

**追記だけ。過去の行を書き換えないこと。**

`state.json` は「いまどうなっているか」を持ちます。上書きされるので、そこには
**経緯**が残りません。なぜその設計にしたのか、何を諦めたのか、どこで方針を変えたのか。
それを失うと、次の周が同じ検討をやり直すか、もっと悪いことに、理由を知らないまま
前の判断をひっくり返します。

1周につき数行で構いません。長さより、**書いてあること**です。

書く価値があるのは、コードを読んでも分からないことだけです。何を変えたかは diff に
あります。ここに要るのは **なぜ** の側です。

---

## 2026-08-03

- `loop/` を用意した。周回をまたいで持ち越すための器（`GOAL.md` / `state.json` /
  `JOURNAL.md`）と、下読みを省くための `map.mjs`、締める前の確認 `check.mjs`。
- 最初に「劣らないクオリティ」を24項目に分解して固定しようとしたが、それは依頼者の
  指定ではなく作った側の判断だったので取り消した。**固定なのは `GOAL.md` の文面だけ**で、
  その解釈は回す側が決める。`state.json` の `criteria` はそのために空にしてある。
- ロックを45分で失効させているのは、1周が15〜25分だから。落ちたセッションのロックが
  残ってループが止まるほうが、二重に走るより困ると判断した。

- 第31周まで回した側から、この器に中身を入れた。`criteria` は自分の言葉で8つに分解した。
  24項目の版を作った側が「それは依頼者の指定ではない」として取り消した判断は正しいと思う。
  ここに書いたのも私の解釈でしかないので、違うと思えば書き換えてほしい。
- `tried` に入れた3件は、いずれも**直す必要が無かったのに直しにかかりかけた**もの。
  第14周で当て推量から手を入れて5周溶かしてから、毎回まず計測ハーネスを書くようにしている。
  そのぶん1周は長くなるが、戻す作業が消えたぶん速い。
- 計測で3回、自分の見立てが外れた（囲みの重なり・NPCの静止・地下が薄いという読み）。
  外れたときに作り話の修正を入れないことのほうが、当てることより効いている気がする。
- ハーネスが `/tmp` にあるのは危うい。31周ぶんの計測基盤が、コンテナが落ちれば消える。
  次の周でリポジトリへ移すのを `backlog` の先頭に置いた。
- GitHub Pages は28回以上ぜんぶ失敗している。ジョブが2秒で落ちてログも出ないので、
  リポジトリで Pages が有効化されていない症状。私の権限では有効にできないため、
  毎周おわりに単一ファイル版を Artifact へ再公開する形にした（URL は固定）。

- 計測ハーネス約80本のうち23本を `tests/` に移した。残りは一度きりのデバッグ用なので捨てた。
  必要になれば書き直すほうが、腐ったものを抱えるより速い。サーバはテストが自前で立てる
  ようにしたので、事前に何も起動しなくていい。
- 移した過程で実バグが2件出た。どちらも**絵を見ていても気づけない**種類のものだった。
  ひとつは採取物の色の書き忘れ（第24周に足した鉱脈3種）で、視界に入るたび描画が例外で
  中断し、それ以降に積まれるはずのアクター・旅人・鳥がまるごと出ていなかった。画面は
  「敵がいないだけ」に見える。もうひとつは配布版が読み込みのたびに undefined.register で
  落ちていたこと（第27周のビルド定義の取りこぼし）。
  どちらも `errs` を見るようにしただけで出た。**今まで見ていなかったほうが問題**だった。
- そこで `report(errs)` を共通化し、`render1` を足した。世界を102箇所回って例外0件を
  確かめる。未知の採取物を混ぜても止まらないことも見る。
- 「900ms 待てば描画が一周したはず」で書いた測定が 2fps では 2/3 で落ちた。時間ではなく
  状態を待つ `until()` を足して直した。同じ形の測定が他にも残っているはず（backlog）。
- 新機能ではなく足場を直す周にした。31周ぶんの計測が /tmp にあってコンテナと一緒に消える
  状態は、次に何を作るかより先に手を打つべきだと判断した。
- `upg1` の発光テストは、直そうとして4通り試して全部外した（世界を止める・差分を取る・
  立ち位置を作る・カメラを決め打ちする）。カメラの決め打ちは `camera.update` に上書き
  されて逆に悪化し、`flatGround` はカメラが追いつく前に世界を止めて豆粒を撮っていた。
  最後は**画面を撮るのをやめた**。見たいのは「強化した発光が描画まで届くか」なので、
  インスタンスに書かれる発光値を直接見れば足りる。決定的になった（4回連続 31/31）。
  この環境で画面を撮って比べる測定は、原則やらないほうがいい。

- 世界の 27%（深い水 21.6% + 浅瀬 5.3%）が水面なのに、そこに敵も村人も旅人も鳥も
  一匹もいなかった。四分の一が動くもののない板だったので、魚と水鳥を入れた。
- 魚は「跳ねる瞬間だけが水面から見える全部」だと考えて、そこに飛沫と音を寄せた。
  朝夕によく跳ね（活き 1.0）、真昼と深夜は沈む（0.25/0.31）。人が近づくと散って深くへ。
- 水鳥は鳥（第30周）の仕組みをそのまま使い、止まり場を海面にしただけ。飛び立ちは
  土煙でなく飛沫にした。作り直さずに済んだのは、第30周で状態機械を分けておいたおかげ。
- 途中で二つ躓いた。ひとつは走査順で、端から埋めると陸のマスで枠が尽き、水鳥の席が
  残らない。もうひとつが本質的で、**190m 格子の中心だけを見ると池も川も陸に見える**。
  周囲 49 マスの中心がすべて陸（16〜39m）と出た。格子より小さい地形は一点では見えない
  ので、マスの中を数点探す形にした。同じ形の判定が他にもあるかもしれない。
- 水面探索を毎フレーム 49 マスぶん回して、鳥の更新を 77µs から 342µs に膨らませた。
  テストの「毎フレームの負担」が拾ってくれた。地形は動かないので覚えておけば済む話で、
  第32周に計測をリポジトリへ移していなければ、この回帰は気づかれずに残っていた。

- Pages が動かない理由を読み違えていた。deploy-pages が 30 回以上 2 秒で落ち、ログも
  残らなかったので「Pages が一度も有効化されていない」と決めた。実際は有効で、配信元が
  main に設定されていただけだった。配信元がブランチのとき deploy-pages は成立しない。
  症状は同じでも原因が違う。ログの出ない失敗から原因を一つに決めるべきではなかったし、
  依頼者に設定を確かめてもらえば 6 周ぶん早く済んだ。
- 配信元が作業ブランチに変わったので、ワークフローは今の設定では永久に赤いままになる。
  消した（必要なら履歴から戻せる。ただし戻すなら配信元を「GitHub Actions」にすること）。
- 公開の迂回（毎周 Artifact へ再公開）はやめた。push すれば数分で
  https://bachikoljunior-blip.github.io/O/ に出る。手元と配信物が 11/11 バイト一致することは
  確認した。sw.js の CACHE を毎周上げるのは続ける——上げないと古いものが残る。
