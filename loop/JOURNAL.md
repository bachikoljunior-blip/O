# 日誌

**追記だけ。過去の行を書き換えないこと。**

`state.json` は「いまどうなっているか」を持ちます。上書きされるので、そこには
**経緯**が残りません。なぜその設計にしたのか、何を諦めたのか、どこで方針を変えたのか。
それを失うと、次の周が同じ検討をやり直すか、もっと悪いことに、理由を知らないまま
前の判断をひっくり返します。

1周につき数行で構いません。長さより、**書いてあること**です。

書く価値があるのは、コードを読んでも分からないことだけです。何を変えたかは diff に
あります。ここに要るのは **なぜ** の側です。

---

## 2026-08-03

- `loop/` を用意した。周回をまたいで持ち越すための器（`GOAL.md` / `state.json` /
  `JOURNAL.md`）と、下読みを省くための `map.mjs`、締める前の確認 `check.mjs`。
- 最初に「劣らないクオリティ」を24項目に分解して固定しようとしたが、それは依頼者の
  指定ではなく作った側の判断だったので取り消した。**固定なのは `GOAL.md` の文面だけ**で、
  その解釈は回す側が決める。`state.json` の `criteria` はそのために空にしてある。
- ロックを45分で失効させているのは、1周が15〜25分だから。落ちたセッションのロックが
  残ってループが止まるほうが、二重に走るより困ると判断した。
