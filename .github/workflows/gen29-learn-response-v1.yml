name: gen29 consume Learn response v1

on:
  push:
    branches:
      - work/gen29-learn-response-v1

permissions:
  contents: write

concurrency:
  group: gen29-learn-response-v1
  cancel-in-progress: false

jobs:
  consume:
    if: ${{ !contains(github.event.head_commit.message, '[native-learn-result]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      GH_TOKEN: ${{ github.token }}
      EXPECTED_MAIN: 62a08a935cbdec8b750c68e115e439bc4db21015
      EXPECTED_STATE_BLOB: 4443a5504aa677b17580a0f1efeee8bc05c7c87f
      EXPECTED_SNAPSHOT_BLOB: ff498ea12645f486272894b0f04356bcf5a4e58f
      EXPECTED_INBOX_BLOB: 93889b482c5b54dd90944f19ce425e845f455b35
      EXPECTED_INBOX_REVISION: '35'
      LEARN_NATIVE_ID: invoke-c0eb5312d7caf14bc2e8fd14
      LEARN_NATIVE_BLOB: e3d2cb24d5e90901df31ada4bd7a67b149d50174
      LEARN_WORK_ID: invoke-60202d63497e24f6a9f87aeb
      LEARN_REQUEST_BLOB: 2019c8a80550b592a3390a2077f7754bc2f040d3
      LEARN_REQUEST_DIGEST: 7a9f79d4ddcc430a922d4c1130e384df08637b1b4fb6b4cae58a2aa625dbb84a
      SOURCE_CI_RUN_ID: '33238893400'
      SOURCE_CI_HEAD: 614ead18e8adbeea313473b885a925a705e12afc
      RUN_ID: run-work-recovery-gen9-durability-repair
      EPISODE_ID: episode-work-recovery-gen9-durability-repair
      EXECUTOR_BINDING: current_chatgpt_work_session
      MODEL_IDENTITY: chatgpt-work-model-unverified
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - name: Install exact repository runtime
        run: python -m pip install -e .

      - name: Assert exact branch, runtime main, authority, and Learn binding
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main --no-tags
          RUNTIME_MAIN="$(git rev-parse origin/main)"
          test "$(git merge-base "$EXPECTED_MAIN" HEAD)" = "$EXPECTED_MAIN"
          python - <<'PY'
          import subprocess
          expected = ['.github/workflows/gen29-learn-response-v1.yml']
          observed = subprocess.check_output(
              ['git', 'diff', '--name-only', __import__('os').environ['EXPECTED_MAIN'], 'HEAD'],
              text=True,
          ).splitlines()
          if observed != expected:
              raise SystemExit(f'launch delta mismatch: {observed!r}')
          PY
          python - "$EXPECTED_MAIN" "$RUNTIME_MAIN" <<'PY'
          import subprocess
          import sys
          base, latest = sys.argv[1:]
          if subprocess.check_output(['git', 'merge-base', base, latest], text=True).strip() != base:
              raise SystemExit('runtime main lost expected ancestry')
          for line in subprocess.check_output(['git', 'diff', '--name-status', base, latest], text=True).splitlines():
              status, path = line.split('\t', 1)
              allowed = (
                  (status == 'M' and path == 'agi/WORK_EXECUTION_STATE.json')
                  or (status == 'A' and path.startswith('note/automation_control/receipts/') and path.endswith('.json'))
                  or (status == 'A' and path.startswith('note/research_workers_clean_g1/evaluation/checkpoint_') and path.endswith('_control_missing.json'))
              )
              if not allowed:
                  raise SystemExit(f'unsafe runtime-main delta: {status} {path}')
          PY
          gh api "repos/$GITHUB_REPOSITORY/contents/agi/WORK_EXECUTION_STATE.json?ref=$RUNTIME_MAIN" > /tmp/runtime-state-document.json
          gh api "repos/$GITHUB_REPOSITORY/contents/agi/USER_INPUT_INBOX.json?ref=$RUNTIME_MAIN" > /tmp/runtime-inbox-document.json
          RUNTIME_STATE_BLOB="$(jq -r '.sha' /tmp/runtime-state-document.json)"
          test "$RUNTIME_STATE_BLOB" = "$EXPECTED_STATE_BLOB"
          test "$(jq -r '.sha' /tmp/runtime-inbox-document.json)" = "$EXPECTED_INBOX_BLOB"
          jq -r '.content' /tmp/runtime-state-document.json | tr -d '\n' | base64 --decode > agi/WORK_EXECUTION_STATE.json
          jq -r '.content' /tmp/runtime-inbox-document.json | tr -d '\n' | base64 --decode > /tmp/runtime-inbox.json
          test "$(git hash-object agi/WORK_EXECUTION_STATE.json)" = "$EXPECTED_STATE_BLOB"
          test "$(git hash-object /tmp/runtime-inbox.json)" = "$EXPECTED_INBOX_BLOB"
          test "$(git hash-object .continual/runs/$RUN_ID/snapshot.json)" = "$EXPECTED_SNAPSHOT_BLOB"
          test "$(git hash-object .continual/work-model/invocations/$LEARN_WORK_ID/request.json)" = "$LEARN_REQUEST_BLOB"
          test "$(git hash-object .continual/runs/$RUN_ID/invocations/$LEARN_NATIVE_ID.json)" = "$LEARN_NATIVE_BLOB"
          echo "RUNTIME_MAIN=$RUNTIME_MAIN" >> "$GITHUB_ENV"
          echo "RUNTIME_STATE_BLOB=$RUNTIME_STATE_BLOB" >> "$GITHUB_ENV"
          python - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path
          from continual.work_session import pending_work_invocations, verified_work_request, verify_work_invocations
          root = Path('.').resolve()
          run_id = os.environ['RUN_ID']
          native_id = os.environ['LEARN_NATIVE_ID']
          work_id = os.environ['LEARN_WORK_ID']
          state = json.loads((root / 'agi/WORK_EXECUTION_STATE.json').read_text())
          inbox = json.loads(Path('/tmp/runtime-inbox.json').read_text())
          snapshot = json.loads((root / '.continual' / 'runs' / run_id / 'snapshot.json').read_text())
          request = verified_work_request(root, work_id)
          native_path = root / '.continual' / 'runs' / run_id / 'invocations' / f'{native_id}.json'
          native = json.loads(native_path.read_text())
          if state.get('execution_id') != 'work-recovery-20260828T122724304Z-b9f52bccaba0472923908d65b1fb59c3':
              raise SystemExit('execution mismatch')
          if state.get('lease_generation') != 29 or hashlib.sha256(state['fence_token'].encode()).hexdigest() != 'c03cdd410193ed54352ee4fd5480cf5e4fccb77d5a1aca67809b7c3e4341890d':
              raise SystemExit('generation/fence mismatch')
          if state.get('status') != 'running' or state.get('resume_required') is not True:
              raise SystemExit('authority is not resumable running state')
          if state.get('active_component') != 'generation29_learn_pending_native_consume':
              raise SystemExit('active component mismatch')
          exact = state.get('exact_continuation', {})
          expected = {
              'completed_native_invocation_id': 'invoke-c60ebf5dbee7ab567f3cee7c',
              'completed_work_invocation_id': 'invoke-c4be414873dff6ba0d4437c6',
              'completed_response_digest': 'd9931fd1f3c2c4e769aa6b8f353962a8dd19ebb2921faf84e2ecb8681cec6dde',
              'pending_native_invocation_id': native_id,
              'pending_native_invocation_ref': f'.continual/runs/{run_id}/invocations/{native_id}.json',
              'pending_native_invocation_blob_sha': os.environ['LEARN_NATIVE_BLOB'],
              'pending_work_invocation_id': work_id,
              'pending_request_ref': f'.continual/work-model/invocations/{work_id}/request.json',
              'pending_request_blob_sha': os.environ['LEARN_REQUEST_BLOB'],
              'pending_request_digest': os.environ['LEARN_REQUEST_DIGEST'],
              'pending_response_ref': None,
              'pending_response_blob_sha': None,
              'pending_response_digest': None,
              'response_exists_on_authoritative_remote': False,
              'snapshot_revision': 97,
              'native_phase': 'post_task_learn_pending',
              'pending_component': 'learn',
              'durability_status': 'pending_native_consume',
          }
          for key, value in expected.items():
              if exact.get(key) != value:
                  raise SystemExit(f'exact continuation mismatch {key}: {exact.get(key)!r} != {value!r}')
          bind = state.get('generation29_consolidate_consume_v1', {})
          if bind.get('status') != 'merged_to_main_verified_learn_bound_pending_native_consume':
              raise SystemExit('Learn state binding status mismatch')
          if bind.get('pending_learn_work_invocation_id') != work_id or bind.get('pending_learn_native_invocation_id') != native_id:
              raise SystemExit('Learn state identity mismatch')
          durability = state.get('continuation_durability', {})
          if durability.get('status') != 'remote_main_readback_verified' or durability.get('verified_remote_readback') is not True:
              raise SystemExit('continuation durability is not verified')
          if durability.get('pending_work_invocation_id') != work_id or durability.get('pending_native_invocation_id') != native_id:
              raise SystemExit('continuation durability identity mismatch')
          if inbox.get('revision') != 35:
              raise SystemExit('inbox revision mismatch')
          if snapshot.get('revision') != 97 or snapshot.get('phase') != 'post_task_learn_pending':
              raise SystemExit('snapshot is not revision97 post_task_learn_pending')
          if request.get('component') != 'learn' or request.get('request_digest') != os.environ['LEARN_REQUEST_DIGEST']:
              raise SystemExit('Learn request mismatch')
          if request.get('prompt_path') != 'prompts/learn.md':
              raise SystemExit('Learn prompt mismatch')
          if request.get('payload', {}).get('preflight_selection', {}).get('decision') != 'USE_ACTIVE':
              raise SystemExit('Learn preflight is not USE_ACTIVE')
          if request.get('executor_binding') != os.environ['EXECUTOR_BINDING'] or request.get('model_identity') != os.environ['MODEL_IDENTITY']:
              raise SystemExit('Learn identity mismatch')
          if (root / '.continual' / 'work-model' / 'invocations' / work_id / 'response.json').exists():
              raise SystemExit('Learn already answered')
          if native.get('status') != 'awaiting_work_model' or native.get('component') != 'learn':
              raise SystemExit('Learn native is not awaiting')
          if native.get('work_invocation_id') != work_id or native.get('work_request_digest') != request.get('request_digest'):
              raise SystemExit('native/Work binding mismatch')
          pending = pending_work_invocations(root, run_id=run_id)
          if len(pending) != 1 or pending[0].get('invocation_id') != work_id:
              raise SystemExit('Learn is not the unique pending Work item')
          verified = verify_work_invocations(root, run_id=run_id)
          expected_counts = {'valid': True, 'requests': 91, 'responses': 90, 'pending': 1, 'native_completed': 84, 'native_fragments': 84, 'native_local_learn': 78}
          for key, value in expected_counts.items():
              if verified.get(key) != value:
                  raise SystemExit(f'before verification mismatch {key}: {verified.get(key)!r}')
          Path('/tmp/expected-before.json').write_text(json.dumps(expected_counts, sort_keys=True) + '\n')
          PY
          continual --root . work-checkpoint-verify --state agi/WORK_EXECUTION_STATE.json > /tmp/work-checkpoint-before.json
          cat /tmp/work-checkpoint-before.json

      - name: Record fresh exact main and exact-head CI observations
        shell: bash
        run: |
          set -euo pipefail
          continual --root . work-source-observation-prepare "$RUN_ID" --state-blob-sha "$RUNTIME_STATE_BLOB" --expected-commit-sha "$RUNTIME_MAIN" --model-identity "$MODEL_IDENTITY" > /tmp/work-source-prepare.json
          python - <<'PY'
          import base64
          import json
          from pathlib import Path
          from continual.store import Store
          document = json.loads(Path('/tmp/runtime-state-document.json').read_text())
          state = json.loads(base64.b64decode(''.join(document['content'].splitlines())).decode())
          projection = {
              'status': state['status'], 'owner_kind': state['owner_kind'],
              'execution_id': state['execution_id'], 'lease_generation': state['lease_generation'],
              'fence_token_digest': Store(Path('.')).stable_digest(state['fence_token'], length=64),
              'heartbeat_at': state['heartbeat_at'],
          }
          Path('/tmp/work-source-projection.json').write_text(json.dumps(projection, sort_keys=True) + '\n')
          PY
          WORK_OBSERVATION_ID="$(jq -r '.observation_id' /tmp/work-source-prepare.json)"
          WORK_REQUEST_DIGEST="$(jq -r '.request_digest' /tmp/work-source-prepare.json)"
          OBSERVED_AT="$(python -c 'from datetime import UTC, datetime; print(datetime.now(UTC).isoformat().replace("+00:00", "Z"))')"
          continual --root . work-source-observation-record "$RUN_ID" "$WORK_OBSERVATION_ID" --request-digest "$WORK_REQUEST_DIGEST" --commit-sha "$RUNTIME_MAIN" --blob-sha "$RUNTIME_STATE_BLOB" --projection /tmp/work-source-projection.json --observed-at "$OBSERVED_AT" --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/work-source-receipt.json

          continual --root . ci-source-observation-prepare "$RUN_ID" --model-identity "$MODEL_IDENTITY" > /tmp/ci-source-prepare.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$SOURCE_CI_RUN_ID" > /tmp/workflow-run-full.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$SOURCE_CI_RUN_ID/jobs?per_page=100" > /tmp/workflow-jobs-full.json
          test "$(jq -r '.head_sha' /tmp/workflow-run-full.json)" = "$SOURCE_CI_HEAD"
          test "$(jq -r '.status' /tmp/workflow-run-full.json)" = "completed"
          test "$(jq -r '.conclusion' /tmp/workflow-run-full.json)" = "success"
          jq -e '[.jobs[] | select(.name == "pytest shard 0 of 4" or .name == "pytest shard 1 of 4" or .name == "pytest shard 2 of 4" or .name == "pytest shard 3 of 4" or .name == "test") | select(.status == "completed" and .conclusion == "success")] | length == 5' /tmp/workflow-jobs-full.json
          jq '{id, workflow_id, name, status, conclusion, head_sha}' /tmp/workflow-run-full.json > /tmp/workflow-run-projection.json
          jq '[.jobs[] | {id, name, status, conclusion}] | sort_by(.id)' /tmp/workflow-jobs-full.json > /tmp/workflow-jobs-projection.json
          CI_OBSERVATION_ID="$(jq -r '.observation_id' /tmp/ci-source-prepare.json)"
          CI_REQUEST_DIGEST="$(jq -r '.request_digest' /tmp/ci-source-prepare.json)"
          OBSERVED_AT="$(python -c 'from datetime import UTC, datetime; print(datetime.now(UTC).isoformat().replace("+00:00", "Z"))')"
          continual --root . ci-source-observation-record "$RUN_ID" "$CI_OBSERVATION_ID" --request-digest "$CI_REQUEST_DIGEST" --workflow-run /tmp/workflow-run-projection.json --jobs /tmp/workflow-jobs-projection.json --observed-at "$OBSERVED_AT" --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/ci-source-receipt.json

      - name: Recheck CAS and continuity before mutation
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main --no-tags
          LATEST_MAIN="$(git rev-parse origin/main)"
          python - "$EXPECTED_MAIN" "$LATEST_MAIN" <<'PY'
          import subprocess
          import sys
          base, latest = sys.argv[1:]
          if subprocess.check_output(['git', 'merge-base', base, latest], text=True).strip() != base:
              raise SystemExit('latest main lost expected ancestry')
          for line in subprocess.check_output(['git', 'diff', '--name-status', base, latest], text=True).splitlines():
              status, path = line.split('\t', 1)
              allowed = (
                  (status == 'M' and path == 'agi/WORK_EXECUTION_STATE.json')
                  or (status == 'A' and path.startswith('note/automation_control/receipts/') and path.endswith('.json'))
                  or (status == 'A' and path.startswith('note/research_workers_clean_g1/evaluation/checkpoint_') and path.endswith('_control_missing.json'))
              )
              if not allowed:
                  raise SystemExit(f'unsafe latest-main delta: {status} {path}')
          PY
          gh api "repos/$GITHUB_REPOSITORY/contents/agi/WORK_EXECUTION_STATE.json?ref=$LATEST_MAIN" > /tmp/pre-mutation-state-document.json
          test "$(jq -r '.sha' /tmp/pre-mutation-state-document.json)" = "$RUNTIME_STATE_BLOB"
          python - <<'PY'
          import json
          import os
          from pathlib import Path
          from continual.ci_source_observation import verify_ci_source_observation
          from continual.continuity_preflight import assert_work_resume_continuity_preflight
          from continual.store import Store
          from continual.work_source_observation import verify_work_source_observation
          root = Path('.').resolve()
          state = json.loads((root / 'agi/WORK_EXECUTION_STATE.json').read_text())
          now = Store(root).utc_now()
          work = verify_work_source_observation(root, run_id=os.environ['RUN_ID'], state=state, state_blob_sha=os.environ['RUNTIME_STATE_BLOB'], now=now)
          ci = verify_ci_source_observation(root, run_id=os.environ['RUN_ID'], state=state, now=now)
          continuity = assert_work_resume_continuity_preflight(root, run_id=os.environ['RUN_ID'], executor_binding=os.environ['EXECUTOR_BINDING'], model_identity=os.environ['MODEL_IDENTITY'])
          if continuity.get('resume_authorized') is not True:
              raise SystemExit('continuity preflight denied')
          print(json.dumps({'work': work, 'ci': ci, 'continuity': continuity}, sort_keys=True))
          PY
          continual --root . work-verify --run-id "$RUN_ID" > /tmp/work-verify-before.json
          continual --root . work-pending --run-id "$RUN_ID" > /tmp/work-pending-before.json


      - name: Submit exact Learn response and freeze Root
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/learn-output.json <<'JSON'
          {
            "result": {
              "decision": "ADD_INACTIVE_CANDIDATE",
              "reason": "The exact episode supplies repeatable evidence that one bounded two-hop retrieval variant improved declared-source recall on three frozen fixtures while preserving the checked stale-source and authority-conflict rejections. The existing Candidate index has no Candidate for this mechanism, and the Execute Local Learn proposal did not register because it lacked an affected component. The evidence supports an inactive Execute Candidate for broader independent validation, but not activation, production routing, or generalization.",
              "candidates": [
                {
                  "candidate_id": "candidate-bounded-recursive-context-retrieval-v1",
                  "target_component": "execute",
                  "expected_scope": "agi/context-kernel/recursive-context-retrieval",
                  "status": "candidate",
                  "prompt_mode": "overlay",
                  "prompt_content": "When an Execute unit explicitly targets Context Kernel retrieval breadth and exact frozen source graphs are available, preserve the current flat selector as the uncontaminated baseline and trial a deterministic bounded two-hop traversal only in non-production. Before admitting any source, preserve the existing provenance, freshness, invalidation, and Work-authority rejection checks. Record flat and recursive required-source recall, every rejected competitor and exact reason, unsafe admissions, deterministic replay stability, and protected-test results. Do not change production routing or infer generalized retrieval reliability from fixture evidence. Require broader independent validation before any scoped activation, and roll back to the flat selector on any unsafe admission, weakened rejection, nondeterminism, or unacceptable measured cost.",
                  "what_it_changes": "Adds a reversible Execute overlay for independently validating bounded recursive Context Kernel retrieval while keeping the flat selector and all fail-closed admission gates as the baseline.",
                  "applicability": [
                    "agi/context-kernel/recursive-context-retrieval",
                    "non-production deterministic retrieval experiments with frozen relation graphs",
                    "independent follow-up validation of missing declared transitive dependencies"
                  ],
                  "non_applicability": [
                    "production routing without separate authorization and validation",
                    "arbitrary graph or generalized retrieval reliability claims",
                    "external capability, AGI, or user-level completion evidence",
                    "units that do not explicitly target Context Kernel retrieval"
                  ],
                  "depends_on": [],
                  "conflicts_with": [],
                  "incompatible_with": [],
                  "scope_states": {
                    "agi/context-kernel/recursive-context-retrieval": "NEED_MORE_EVIDENCE",
                    "global": "untested"
                  },
                  "supporting_evidence": [
                    "agi/RECURSIVE_CONTEXT_RETRIEVAL_ABLATION.json: three fixtures; flat mean recall 0.3333333333333333; recursive mean recall 1.0; mean delta 0.6666666666666667",
                    "agi/RECURSIVE_CONTEXT_RETRIEVAL_FIXTURES.json: zero unsafe admissions with stale_at_decision_time and authority_conflict:work-state->o-work-mode-monitor preserved",
                    "tests/test_recursive_context_retrieval_ablation.py: six byte-stable replays and three fixture PASS verdicts",
                    ".continual/episodes/episode-work-recovery-gen9-durability-repair/episode.json: bounded unit PASS separated from task/user FAIL and agi_claim_supported=false"
                  ],
                  "contradictory_evidence": [
                    "Only three frozen repository fixtures were tested.",
                    "No production workload, arbitrary graph, independent external evaluator, latency bound, or tool-cost bound was established.",
                    "No evidence authorizes production or scoped activation."
                  ],
                  "tested_with": [
                    "unit-generation25-recursive-context-retrieval-ablation-v1",
                    "unit-generation29-evaluate-recursive-context-retrieval-ablation-v1"
                  ],
                  "risk": "A recursive overlay can widen context, admit stale or authority-conflicting sources, hide nondeterminism, or consume excessive latency/tokens if its bounds or admission gates are weakened.",
                  "rollback_condition": "Return to the unchanged flat selector and mark the Candidate REJECTED_FOR_SCOPE for any unsafe admission, rejection-regression, replay mismatch, protected-test failure, or measured cost outside a precommitted bound.",
                  "cost": "Additional bounded traversal and comparison work; production cost is intentionally unmeasured and unauthorized."
                }
              ],
              "existing_candidate_actions": [
                {
                  "candidate_id": "candidate-task-evaluate-unit-verdict-v1",
                  "action": "RETAIN_CANDIDATE",
                  "scope": "continual/incremental-task-evaluation",
                  "reason": "The scoped trial preserved unit PASS independently from task/user FAIL, but protected-baseline VERIFIED_FOR_SCOPE evidence is still absent; neither scoped nor global activation is authorized."
                }
              ],
              "retained_learning": [
                "Bounded recursion can be informative only when the flat selector remains an exact comparison baseline and every admission and rejection is auditable.",
                "Recall gain and zero unsafe admission must be carried together; either alone is insufficient.",
                "Fixture PASS, Candidate state, production routing, independent capability, AGI, and user-level completion are separate claims.",
                "A Candidate proposal must name its target component and reversible prompt behavior to become durable Candidate state."
              ],
              "claim_boundary": "This Learn decision is supported only by the exact three frozen fixtures, deterministic non-production implementation, reviewed immutable episode, and request-supplied lifecycle evidence. It does not establish arbitrary-graph reliability, production fitness, independent external capability, AGI, or completion of the user's upper objective.",
              "activation_authorized": false,
              "production_routing_change_authorized": false,
              "next": "After exact-head publication and remote-main readback of the current lifecycle, return to Root and select a materially different safe unit. The new Candidate remains inactive and should be trialed only if a future unit explicitly targets broader independent recursive-retrieval validation."
            },
            "fragment": {
              "component": "learn",
              "purpose": "Retain the bounded recursive-retrieval lesson as one inactive, reversible Execute Candidate without widening the three-fixture evidence.",
              "evidence_refs": [
                "agi/RECURSIVE_CONTEXT_RETRIEVAL_ABLATION.json",
                "agi/RECURSIVE_CONTEXT_RETRIEVAL_FIXTURES.json",
                "tests/test_recursive_context_retrieval_ablation.py",
                ".continual/episodes/episode-work-recovery-gen9-durability-repair/episode.json",
                ".continual/candidates/index.json"
              ],
              "observations": [
                "The recursive variant improved declared-source recall from one third to 1.0 on exactly three frozen fixtures with zero unsafe admissions and six byte-stable replays.",
                "The existing Candidate index contains no recursive-retrieval Candidate, while the episode records an inactive proposal requiring broader independent validation.",
                "candidate-task-evaluate-unit-verdict-v1 remains candidate-only because activation evidence is incomplete.",
                "No production, generalized, external-capability, AGI, or user-completion claim is supported."
              ],
              "unresolved": [
                "exact-head publication and remote-main readback of the current Consolidate and Learn lifecycle",
                "broader independent validation with precommitted cost and safety bounds before any Candidate activation",
                "a materially different Root-selected unit and the user's unchanged upper objective"
              ]
            }
          }
          JSON
          python -m json.tool /tmp/learn-output.json > /dev/null
          continual --root . work-submit "$LEARN_WORK_ID" --response /tmp/learn-output.json --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/learn-submit.json
          continual --root . work-resume "$RUN_ID" --max-steps 2 --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/native-resume.json
          continual --root . work-verify --run-id "$RUN_ID" > /tmp/work-verify-after.json
          continual --root . work-pending --run-id "$RUN_ID" > /tmp/work-pending-after.json
          cat /tmp/learn-submit.json
          cat /tmp/native-resume.json
          cat /tmp/work-verify-after.json
          cat /tmp/work-pending-after.json

      - name: Validate exact Learn completion, Candidate registration, and Root freeze
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import os
          import subprocess
          from datetime import UTC, datetime
          from pathlib import Path
          from continual.work_session import pending_work_invocations, verified_work_invocation, verify_work_invocations
          root = Path('.').resolve()
          run_id = os.environ['RUN_ID']
          native_id = os.environ['LEARN_NATIVE_ID']
          work_id = os.environ['LEARN_WORK_ID']
          expected_output = json.loads(Path('/tmp/learn-output.json').read_text())
          before = json.loads(Path('/tmp/work-verify-before.json').read_text())
          expected_before = {'valid': True, 'requests': 91, 'responses': 90, 'pending': 1, 'native_completed': 84, 'native_fragments': 84, 'native_local_learn': 78}
          for key, value in expected_before.items():
              if before.get(key) != value:
                  raise SystemExit(f'before mismatch {key}: {before.get(key)!r}')
          after = verify_work_invocations(root, run_id=run_id)
          expected_after = {'valid': True, 'requests': 92, 'responses': 91, 'pending': 1, 'native_completed': 85, 'native_fragments': 85, 'native_local_learn': 78}
          for key, value in expected_after.items():
              if after.get(key) != value:
                  raise SystemExit(f'after mismatch {key}: {after.get(key)!r} != {value!r}')
          verified = verified_work_invocation(root, work_id)
          if verified.get('output') != expected_output:
              raise SystemExit('persisted Learn output mismatch')
          native_path = root / '.continual' / 'runs' / run_id / 'invocations' / f'{native_id}.json'
          native = json.loads(native_path.read_text())
          if native.get('status') != 'complete' or native.get('component') != 'learn' or native.get('output') != expected_output:
              raise SystemExit('Learn native completion mismatch')
          response_rel = f'.continual/work-model/invocations/{work_id}/response.json'
          fragment_rel = native.get('fragment_ref')
          if not fragment_rel or not (root / fragment_rel).is_file():
              raise SystemExit('Learn fragment missing')
          if (root / '.continual' / 'runs' / run_id / 'local-learn' / f'{native_id}-learn.json').exists():
              raise SystemExit('Learn recursively wrote Local Learn')
          artifact_rel = f'.continual/runs/{run_id}/artifacts/post-task-learn.json'
          if json.loads((root / artifact_rel).read_text()) != expected_output['result']:
              raise SystemExit('post-task Learn artifact mismatch')
          candidate_id = 'candidate-bounded-recursive-context-retrieval-v1'
          candidate_rel = f'.continual/candidates/{candidate_id}/candidate.json'
          prompt_rel = f'.continual/candidates/{candidate_id}/prompt.md'
          index_rel = '.continual/candidates/index.json'
          candidate = json.loads((root / candidate_rel).read_text())
          expected_proposal = expected_output['result']['candidates'][0]
          for key, value in expected_proposal.items():
              if key == 'prompt_content':
                  continue
              if candidate.get(key) != value:
                  raise SystemExit(f'Candidate mismatch {key}: {candidate.get(key)!r} != {value!r}')
          if candidate.get('prompt_path') != prompt_rel:
              raise SystemExit('Candidate prompt path mismatch')
          if (root / prompt_rel).read_text().strip() != expected_proposal['prompt_content'].strip():
              raise SystemExit('Candidate prompt content mismatch')
          index = json.loads((root / index_rel).read_text())
          items = [item for item in index.get('candidates', []) if item.get('candidate_id') == candidate_id]
          if len(items) != 1 or items[0].get('status') != 'candidate' or items[0].get('target_component') != 'execute':
              raise SystemExit('Candidate index registration mismatch')
          relations_rel = f'.continual/episodes/{os.environ["EPISODE_ID"]}/relations.jsonl'
          produced = []
          for line in (root / relations_rel).read_text().splitlines():
              if line.strip():
                  value = json.loads(line)
                  if value.get('type') == 'produced_candidate' and value.get('candidate_id') == candidate_id:
                      produced.append(value)
          if len(produced) != 1:
              raise SystemExit(f'expected one produced-candidate relation, got {len(produced)}')
          snapshot_rel = f'.continual/runs/{run_id}/snapshot.json'
          snapshot = json.loads((root / snapshot_rel).read_text())
          if snapshot.get('revision') != 98 or snapshot.get('phase') != 'root_pending' or snapshot.get('status') != 'continue':
              raise SystemExit(f'bad revision98 Root phase: {snapshot.get("revision")!r} {snapshot.get("phase")!r} {snapshot.get("status")!r}')
          if snapshot.get('produced_candidates') != [candidate_id]:
              raise SystemExit('snapshot produced_candidates mismatch')
          pending = pending_work_invocations(root, run_id=run_id)
          if len(pending) != 1:
              raise SystemExit(f'expected one Root request, got {len(pending)}')
          root_request = pending[0]
          if root_request.get('component') != 'root' or root_request.get('prompt_path') != 'prompts/root.md':
              raise SystemExit('next pending component is not Root')
          if root_request.get('executor_binding') != os.environ['EXECUTOR_BINDING'] or root_request.get('model_identity') != os.environ['MODEL_IDENTITY']:
              raise SystemExit('Root identity mismatch')
          payload = root_request.get('payload', {})
          if payload.get('snapshot', {}).get('revision') != 98 or payload.get('snapshot', {}).get('phase') != 'root_pending':
              raise SystemExit('Root payload snapshot mismatch')
          if payload.get('preflight_selection', {}).get('decision') != 'USE_ACTIVE':
              raise SystemExit('Root preflight did not retain active prompt')
          root_id = root_request['invocation_id']
          root_rel = f'.continual/work-model/invocations/{root_id}/request.json'
          if (root / root_rel).with_name('response.json').exists():
              raise SystemExit('Root already answered')
          awaiting = []
          for path in sorted((root / '.continual' / 'runs' / run_id / 'invocations').glob('*.json')):
              value = json.loads(path.read_text())
              if value.get('status') == 'awaiting_work_model':
                  awaiting.append((path, value))
          if len(awaiting) != 1:
              raise SystemExit(f'expected one awaiting Root native journal, got {len(awaiting)}')
          root_native_path, root_native = awaiting[0]
          if root_native.get('component') != 'root' or root_native.get('work_invocation_id') != root_id or root_native.get('work_request_digest') != root_request.get('request_digest'):
              raise SystemExit('Root native/Work binding mismatch')
          summary = {
              'learn_native_invocation_id': native_id,
              'learn_work_invocation_id': work_id,
              'learn_response_ref': response_rel,
              'learn_response_digest': verified['response']['response_digest'],
              'learn_response_blob_sha': subprocess.check_output(['git','hash-object',response_rel], text=True).strip(),
              'learn_native_ref': native_path.relative_to(root).as_posix(),
              'learn_native_blob_sha': subprocess.check_output(['git','hash-object',str(native_path)], text=True).strip(),
              'learn_fragment_ref': fragment_rel,
              'learn_fragment_blob_sha': subprocess.check_output(['git','hash-object',fragment_rel], text=True).strip(),
              'learn_artifact_ref': artifact_rel,
              'learn_artifact_blob_sha': subprocess.check_output(['git','hash-object',artifact_rel], text=True).strip(),
              'candidate_id': candidate_id,
              'candidate_ref': candidate_rel,
              'candidate_blob_sha': subprocess.check_output(['git','hash-object',candidate_rel], text=True).strip(),
              'candidate_prompt_ref': prompt_rel,
              'candidate_prompt_blob_sha': subprocess.check_output(['git','hash-object',prompt_rel], text=True).strip(),
              'candidate_index_ref': index_rel,
              'candidate_index_blob_sha': subprocess.check_output(['git','hash-object',index_rel], text=True).strip(),
              'relations_ref': relations_rel,
              'relations_blob_sha': subprocess.check_output(['git','hash-object',relations_rel], text=True).strip(),
              'snapshot_ref': snapshot_rel,
              'snapshot_blob_sha': subprocess.check_output(['git','hash-object',snapshot_rel], text=True).strip(),
              'snapshot_revision': 98,
              'snapshot_phase': 'root_pending',
              'pending_work_invocation_id': root_id,
              'pending_request_ref': root_rel,
              'pending_request_digest': root_request['request_digest'],
              'pending_request_blob_sha': subprocess.check_output(['git','hash-object',root_rel], text=True).strip(),
              'pending_native_invocation_id': root_native['invocation_id'],
              'pending_native_invocation_ref': root_native_path.relative_to(root).as_posix(),
              'pending_native_invocation_blob_sha': subprocess.check_output(['git','hash-object',str(root_native_path)], text=True).strip(),
              'verification': expected_after,
          }
          Path('/tmp/learn-summary.json').write_text(json.dumps(summary, indent=2, sort_keys=True) + '\n')
          receipt_rel = f'.continual/runs/{run_id}/recovery-receipts/gen29-learn-consume-root-freeze-v1.json'
          receipt = {
              'schema_version': 1,
              'record_type': 'generation29_learn_consume_receipt',
              'recorded_at': datetime.now(UTC).isoformat().replace('+00:00','Z'),
              'authority': {'execution_id':'work-recovery-20260828T122724304Z-b9f52bccaba0472923908d65b1fb59c3','lease_generation':29,'fence_token_digest':'c03cdd410193ed54352ee4fd5480cf5e4fccb77d5a1aca67809b7c3e4341890d'},
              'source_main_sha': os.environ['RUNTIME_MAIN'],
              'source_state_blob_sha': os.environ['RUNTIME_STATE_BLOB'],
              'source_ci_workflow_run_id': int(os.environ['SOURCE_CI_RUN_ID']),
              'idempotency_key': 'o-work-gen29:consume-learn-invoke-60202d63497e24f6a9f87aeb:freeze-root:v1',
              'completed_effect': summary,
              'request_recreated': False,
              'learn_reinvoked': False,
              'candidate_activation_authorized': False,
              'resume_required': True,
              'next_action': 'Exact-head validate and merge this Learn lifecycle plus revision98 Root freeze, bind state, then answer the exact materially different Root request once.',
          }
          receipt_path = root / receipt_rel
          receipt_path.parent.mkdir(parents=True, exist_ok=True)
          receipt_path.write_text(json.dumps(receipt, indent=2, sort_keys=True) + '\n')
          print('LEARN_SUMMARY=' + json.dumps(summary, sort_keys=True))
          PY

      - name: Stage only exact Learn lifecycle delta and push once
        shell: bash
        run: |
          set -euo pipefail
          git checkout -- agi/WORK_EXECUTION_STATE.json
          test "$(git hash-object agi/WORK_EXECUTION_STATE.json)" = "$EXPECTED_STATE_BLOB"
          python - <<'PY'
          import json
          import os
          import subprocess
          from pathlib import Path
          run_id = os.environ['RUN_ID']
          native_id = os.environ['LEARN_NATIVE_ID']
          work_id = os.environ['LEARN_WORK_ID']
          summary = json.loads(Path('/tmp/learn-summary.json').read_text())
          work_obs = json.loads(Path('/tmp/work-source-prepare.json').read_text())['observation_id']
          ci_obs = json.loads(Path('/tmp/ci-source-prepare.json').read_text())['observation_id']
          receipt_rel = f'.continual/runs/{run_id}/recovery-receipts/gen29-learn-consume-root-freeze-v1.json'
          expected = {
              '.continual/candidates/index.json': 'M',
              '.continual/candidates/candidate-bounded-recursive-context-retrieval-v1/candidate.json': 'A',
              '.continual/candidates/candidate-bounded-recursive-context-retrieval-v1/prompt.md': 'A',
              f'.continual/episodes/{os.environ["EPISODE_ID"]}/relations.jsonl': 'M',
              f'.continual/runs/{run_id}/artifacts/post-task-learn.json': 'M',
              f'.continual/runs/{run_id}/events.jsonl': 'M',
              f'.continual/runs/{run_id}/snapshot.json': 'M',
              f'.continual/runs/{run_id}/invocations/{native_id}.json': 'M',
              f'.continual/work-model/invocations/{work_id}/response.json': 'A',
              summary['learn_fragment_ref']: 'A',
              summary['pending_native_invocation_ref']: 'A',
              summary['pending_request_ref']: 'A',
              f'.continual/runs/{run_id}/work-source-observations/{work_obs}/request.json': 'A',
              f'.continual/runs/{run_id}/work-source-observations/{work_obs}/receipt.json': 'A',
              f'.continual/runs/{run_id}/ci-source-observations/{ci_obs}/request.json': 'A',
              f'.continual/runs/{run_id}/ci-source-observations/{ci_obs}/receipt.json': 'A',
              receipt_rel: 'A',
          }
          status = {}
          for line in subprocess.check_output(['git','diff','--name-status'], text=True).splitlines():
              code, path = line.split('\t',1)
              status[path] = code
          others = (
              subprocess.check_output(['git','ls-files','--others','--exclude-standard'], text=True).splitlines()
              + subprocess.check_output(['git','ls-files','--others','--ignored','--exclude-standard'], text=True).splitlines()
          )
          for path in others:
              status.setdefault(path,'A')
          for path in list(status):
              if path.startswith('src/agi/__pycache__/') or path.startswith('src/continual/__pycache__/') or path.startswith('src/continual_chatgpt.egg-info/'):
                  status.pop(path,None)
          prefix = f'.continual/runs/{run_id}/preflight/preflight-root-'
          preflights = sorted(path for path,code in status.items() if path.startswith(prefix) and path.endswith('.json') and code == 'A')
          if len(preflights) != 1:
              raise SystemExit('expected one new Root preflight: ' + json.dumps(preflights))
          expected[preflights[0]] = 'A'
          if status != expected:
              raise SystemExit('lifecycle mutation mismatch: ' + json.dumps({'expected':expected,'observed':status,'missing':sorted(set(expected)-set(status)),'unexpected':sorted(set(status)-set(expected))}, sort_keys=True))
          required = sorted(expected)
          for path in required:
              if not Path(path).is_file():
                  raise SystemExit('required path missing: ' + path)
          subprocess.run(['git','add','-f','--',*required], check=True)
          staged = set(subprocess.check_output(['git','diff','--cached','--name-only'], text=True).splitlines())
          if staged != set(required):
              raise SystemExit('staged set mismatch')
          immutable = sorted(path for path in staged if path.startswith('.continual/work-model/invocations/'))
          if immutable != sorted([f'.continual/work-model/invocations/{work_id}/response.json', summary['pending_request_ref']]):
              raise SystemExit('immutable mutation set mismatch: ' + json.dumps(immutable))
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m '[native-learn-result] gen29 consume Learn and freeze Root'
          git push origin "HEAD:$GITHUB_REF_NAME"
