name: gen29 consume formal-proof Consolidate and freeze Learn

on:
  push:
    branches:
      - work/gen29-formal-proof-consolidate-v1
    paths:
      - .github/workflows/gen29-formal-proof-consolidate-consume-v1.yml
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: gen29-formal-proof-consolidate-consume-v1
  cancel-in-progress: false

jobs:
  consume:
    if: ${{ !contains(github.event.head_commit.message, '[native-formal-proof-learn-frozen]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      EXPECTED_MAIN: bf1bc92525c624f95d165a8ab7cee8cd5e15eb77
      EXPECTED_STATE_BLOB: bf60ee1c57ade05c44d849dfa3c25d7bfd4fdd06
      RUN_ID: run-work-recovery-gen9-durability-repair
      TASK_NATIVE_ID: invoke-c12c2d3c0055ceeb69e6d9c5
      TASK_WORK_ID: invoke-7d9a5dc703734404449aae3d
      TASK_REQUEST_DIGEST: 87197eca087739bf7fcfc2b4d329e7f8bbc1347da67f76f7097b7285b9d7717a
      EXECUTOR_BINDING: current_chatgpt_work_session
      MODEL_IDENTITY: chatgpt-work-model-unverified
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - run: python -m pip install -e .
      - name: Verify authority and exact frozen Consolidate
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main --no-tags
          test "$(git rev-parse origin/main)" = "$EXPECTED_MAIN"
          test "$(git hash-object agi/WORK_EXECUTION_STATE.json)" = "$EXPECTED_STATE_BLOB"
          test "$(git hash-object .continual/work-model/invocations/$TASK_WORK_ID/request.json)" = "d618d027cf9bea665094bd9b497acdaefb7bbc6c"
          test "$(git hash-object .continual/runs/$RUN_ID/invocations/$TASK_NATIVE_ID.json)" = "3d4a020fbc89df8b7cec464d07f426c62c920767"
          test "$(git hash-object .continual/runs/$RUN_ID/snapshot.json)" = "a578bf53e24fc265eccf1785fa3af0981fb64f77"
          PYTHONPATH=src python -m continual.cli --root . work-pending --run-id "$RUN_ID" > /tmp/pending-before.json
          PYTHONPATH=src python -m continual.cli --root . work-verify --run-id "$RUN_ID" > /tmp/verify-before.json
          python - <<'PY'
          import hashlib,json,os
          from pathlib import Path
          state=json.loads(Path('agi/WORK_EXECUTION_STATE.json').read_text())
          assert state['status']=='running'
          assert state['execution_id']=='work-recovery-20260828T122724304Z-b9f52bccaba0472923908d65b1fb59c3'
          assert state['lease_generation']==29
          assert hashlib.sha256(state['fence_token'].encode()).hexdigest()=='c03cdd410193ed54352ee4fd5480cf5e4fccb77d5a1aca67809b7c3e4341890d'
          assert state['pending_work_invocation_id']==os.environ['TASK_WORK_ID']
          assert state['pending_native_invocation_id']==os.environ['TASK_NATIVE_ID']
          assert state['pending_request_digest']==os.environ['TASK_REQUEST_DIGEST']
          assert state['finalization_readback_verified'] is True
          exact=state['exact_continuation']
          assert exact['status']=='remote_main_readback_verified'
          assert exact['pending_work_invocation_id']==os.environ['TASK_WORK_ID']
          assert exact['pending_native_invocation_id']==os.environ['TASK_NATIVE_ID']
          assert exact['pending_request_digest']==os.environ['TASK_REQUEST_DIGEST']
          assert exact['snapshot_revision']==139 and exact['pending_component']=='consolidate_episode'
          p=json.load(open('/tmp/pending-before.json'))['pending']
          assert len(p)==1 and p[0]['invocation_id']==os.environ['TASK_WORK_ID'],p
          assert p[0]['component']=='consolidate_episode',p
          assert p[0]['payload']['snapshot']['current_unit']=='unit-generation29-evaluate-external-formal-proof-synthesis-v1',p
          v=json.load(open('/tmp/verify-before.json'))
          assert v['valid'] is True and v['pending']==1,v
          response=Path(f".continual/work-model/invocations/{os.environ['TASK_WORK_ID']}/response.json")
          assert not response.exists(),response
          PY
      - name: Refresh exact authoritative heartbeat at runner start
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git fetch origin main --no-tags
          test "$(git rev-parse origin/main)" = "$EXPECTED_MAIN"
          test "$(git hash-object agi/WORK_EXECUTION_STATE.json)" = "$EXPECTED_STATE_BLOB"
          python - <<'PY'
          import json,os
          from datetime import UTC,datetime
          from pathlib import Path
          p=Path('agi/WORK_EXECUTION_STATE.json')
          s=json.loads(p.read_text())
          now=datetime.now(UTC).isoformat().replace('+00:00','Z')
          s['heartbeat_at']=now
          s['last_activity_at']=now
          repair=s['generation29_formal_proof_task_evaluate_consolidate_bind_v1']
          assert repair['status']=='remote_main_readback_verified'
          assert s['pending_work_invocation_id']==os.environ['TASK_WORK_ID']
          assert s['pending_native_invocation_id']==os.environ['TASK_NATIVE_ID']
          assert s['pending_request_digest']==os.environ['TASK_REQUEST_DIGEST']
          refreshes=repair.setdefault('runner_start_refreshes',[])
          refreshes.append({'workflow_run_id':int(os.environ['GITHUB_RUN_ID']),'run_attempt':int(os.environ['GITHUB_RUN_ATTEMPT']),'refreshed_at':now,'reason':'Bound queue delay so Context Kernel sees a current durable owner heartbeat immediately before the unchanged exact consume.'})
          repair['recurrence_validation']='runner-start durable CAS written; exact readback and unchanged-request consume pending'
          repair['next_action']='Exact-read this runner-start refresh, then consume the unchanged Consolidate request once.'
          p.write_text(json.dumps(s,indent=2,sort_keys=True)+'\n')
          PY
          python - <<'PY'
          import base64,json,os
          from pathlib import Path
          payload={
            'message':'o-work: refresh Consolidate heartbeat at runner start',
            'content':base64.b64encode(Path('agi/WORK_EXECUTION_STATE.json').read_bytes()).decode(),
            'sha':os.environ['EXPECTED_STATE_BLOB'],
            'branch':'main',
          }
          Path('/tmp/heartbeat-payload.json').write_text(json.dumps(payload,separators=(',',':')))
          PY
          gh api --method PUT "repos/$GITHUB_REPOSITORY/contents/agi/WORK_EXECUTION_STATE.json" --input /tmp/heartbeat-payload.json > /tmp/heartbeat-write.json
          RUNTIME_MAIN="$(jq -r '.commit.sha' /tmp/heartbeat-write.json)"
          RUNTIME_STATE_BLOB="$(jq -r '.content.sha' /tmp/heartbeat-write.json)"
          gh api "repos/$GITHUB_REPOSITORY/contents/agi/WORK_EXECUTION_STATE.json?ref=$RUNTIME_MAIN" > /tmp/heartbeat-readback.json
          test "$(jq -r '.sha' /tmp/heartbeat-readback.json)" = "$RUNTIME_STATE_BLOB"
          jq -r '.content' /tmp/heartbeat-readback.json | tr -d '\n' | base64 --decode > agi/WORK_EXECUTION_STATE.json
          test "$(git hash-object agi/WORK_EXECUTION_STATE.json)" = "$RUNTIME_STATE_BLOB"
          echo "RUNTIME_MAIN=$RUNTIME_MAIN" >> "$GITHUB_ENV"
          echo "RUNTIME_STATE_BLOB=$RUNTIME_STATE_BLOB" >> "$GITHUB_ENV"
          echo "RUNNER_HEARTBEAT_REFRESHED $RUNTIME_MAIN $RUNTIME_STATE_BLOB"

      - name: Record fresh authoritative Work source observation
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          python - <<'PY'
          import base64,json,os,urllib.request
          from datetime import UTC,datetime
          from pathlib import Path
          from continual.store import Store
          from continual.work_source_observation import prepare_work_source_observation,record_work_source_observation_receipt,verify_work_source_observation
          root=Path('.').resolve();run_id=os.environ['RUN_ID']
          state=json.loads((root/'agi/WORK_EXECUTION_STATE.json').read_text())
          request=prepare_work_source_observation(root,run_id=run_id,state=state,state_blob_sha=os.environ['RUNTIME_STATE_BLOB'],expected_commit_sha=os.environ['RUNTIME_MAIN'],model_identity=os.environ['MODEL_IDENTITY'])
          headers={'Accept':'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28','Authorization':'Bearer '+os.environ['GITHUB_TOKEN']}
          def get(url):
            with urllib.request.urlopen(urllib.request.Request(url,headers=headers),timeout=30) as response:return json.load(response)
          branch=get('https://api.github.com/repos/bachikoljunior-blip/O/branches/main')
          remote=get('https://api.github.com/repos/bachikoljunior-blip/O/contents/agi/WORK_EXECUTION_STATE.json?ref=main')
          assert branch['commit']['sha']==os.environ['RUNTIME_MAIN']
          assert remote['sha']==os.environ['RUNTIME_STATE_BLOB']
          remote_state=json.loads(base64.b64decode(remote['content']).decode())
          store=Store(root)
          projection={'status':remote_state['status'],'owner_kind':remote_state['owner_kind'],'execution_id':remote_state['execution_id'],'lease_generation':remote_state['lease_generation'],'fence_token_digest':store.stable_digest(remote_state['fence_token'],length=64),'heartbeat_at':remote_state['heartbeat_at']}
          observed_at=datetime.now(UTC).isoformat().replace('+00:00','Z')
          receipt=record_work_source_observation_receipt(root,run_id=run_id,observation_id=request['observation_id'],request_digest=request['request_digest'],executor_binding=state['authoritative_source_observation_policy']['executor_binding'],model_identity=os.environ['MODEL_IDENTITY'],commit_sha=branch['commit']['sha'],blob_sha=remote['sha'],projection=projection,observed_at=observed_at)
          verified=verify_work_source_observation(root,run_id=run_id,state=state,state_blob_sha=os.environ['RUNTIME_STATE_BLOB'],now=observed_at)
          assert verified['observation_id']==request['observation_id']
          print(json.dumps({'request':request,'receipt':receipt,'verified':verified},sort_keys=True))
          PY
      - name: Record fresh exact CI source observation
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          python - <<'PY'
          import json,os,urllib.request
          from datetime import UTC,datetime
          from pathlib import Path
          from continual.ci_source_observation import prepare_ci_source_observation,record_ci_source_observation_receipt,verify_ci_source_observation
          root=Path('.').resolve();run_id=os.environ['RUN_ID']
          state=json.loads((root/'agi/WORK_EXECUTION_STATE.json').read_text())
          request=prepare_ci_source_observation(root,run_id=run_id,state=state,model_identity=os.environ['MODEL_IDENTITY'])
          headers={'Accept':'application/vnd.github+json','X-GitHub-Api-Version':'2022-11-28','Authorization':'Bearer '+os.environ['GITHUB_TOKEN']}
          def get(url):
            with urllib.request.urlopen(urllib.request.Request(url,headers=headers),timeout=30) as response:return json.load(response)
          policy=state['ci_source_observation_policy'];rid=policy['workflow_run_id']
          remote_run=get(f'https://api.github.com/repos/bachikoljunior-blip/O/actions/runs/{rid}')
          remote_jobs=get(f'https://api.github.com/repos/bachikoljunior-blip/O/actions/runs/{rid}/jobs?per_page=100')['jobs']
          required={v['id'] for v in policy['required_jobs']}
          jobs=[{'id':v['id'],'name':v['name'],'status':v['status'],'conclusion':v['conclusion']} for v in remote_jobs if v['id'] in required]
          run_projection={'id':remote_run['id'],'workflow_id':remote_run['workflow_id'],'name':remote_run['name'],'status':remote_run['status'],'conclusion':remote_run['conclusion'],'head_sha':remote_run['head_sha']}
          observed_at=datetime.now(UTC).isoformat().replace('+00:00','Z')
          receipt=record_ci_source_observation_receipt(root,run_id=run_id,observation_id=request['observation_id'],request_digest=request['request_digest'],executor_binding=policy['executor_binding'],model_identity=os.environ['MODEL_IDENTITY'],workflow_run=run_projection,jobs=jobs,observed_at=observed_at)
          verified=verify_ci_source_observation(root,run_id=run_id,state=state,now=observed_at)
          assert verified['observation_id']==request['observation_id']
          print(json.dumps({'request':request,'receipt':receipt,'verified':verified},sort_keys=True))
          PY
      - name: Submit exact Consolidate response and freeze Learn
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/task-output.json <<'TASKJSON'
          {
            "result": {
              "episode_id": "episode-work-recovery-gen9-durability-repair",
              "episode_type": "bounded_external_formal_proof_synthesis_and_evaluation",
              "execute_unit_id": "unit-generation29-external-formal-proof-synthesis-v1",
              "evaluation_unit_id": "unit-generation29-evaluate-external-formal-proof-synthesis-v1",
              "status": "continue",
              "episode_summary": "A single immutable Lean theorem was selected before solution access, failed under the sentinel True.intro baseline, solved with one generated proof, and verified by exact compilation, two negative controls, clean immutable replay, digest matching, zero upstream writes, protected exact-head CI, expected-head merge, and remote blob readback. Task Evaluate therefore assigned unit_verdict PASS while keeping the original task, user-level objective, upper objective, and AGI claim at FAIL. Pre-application and post-result Candidate decisions caused no activation or production routing.",
              "claim_boundary": "PASS applies only to Exercises.Analysis.OrderCompleteness.q1_integer_fractional_decomposition at aimmerwahr/lean-math-exercises commit ef9d2506ecf03bb3338e59d33edded195d94a9be, source blob b1472e9b72df28045f33d0270f860ce793ffbd5e, Lean v4.33.0-rc1, and mathlib 4851eb. It excludes generalized theorem proving, held-out breadth, robustness, autonomy, production readiness, self-improvement, AGI, user-level completion, upper-objective completion, and monitor completion.",
              "environment": {
                "model_identity": "chatgpt-work-model-unverified",
                "executor_binding": "current_chatgpt_work_session",
                "run_id": "run-work-recovery-gen9-durability-repair"
              },
              "sequence": [
                {
                  "step": 1,
                  "component": "root",
                  "kind": "routing",
                  "summary": "Selected one immutable external formal-proof synthesis unit with precommit ordering and contamination exclusions."
                },
                {
                  "step": 2,
                  "component": "candidate_evaluate",
                  "kind": "pre_application",
                  "summary": "Retained the baseline-first Execute route; candidate_activation remained false."
                },
                {
                  "step": 3,
                  "component": "execute",
                  "kind": "proof_synthesis",
                  "summary": "Generated one Lean proof for the exact frozen theorem without reading solution-bearing material or writing upstream."
                },
                {
                  "step": 4,
                  "component": "verification",
                  "kind": "bounded_controls",
                  "summary": "Observed sentinel-baseline rejection, candidate compilation, corrupted-proof rejection, changed-statement rejection, clean replay, digest match, and zero upstream writes."
                },
                {
                  "step": 5,
                  "component": "publication",
                  "kind": "protected_exact_head",
                  "summary": "PR416 merged the bounded proof evidence after all required exact-head CI jobs succeeded; PR417 published the pre-application Candidate result; PR418 published the Task Evaluate response and unique Consolidate freeze."
                },
                {
                  "step": 6,
                  "component": "task_evaluate",
                  "invocation_id": "invoke-35f65d7ed8050023a39ab49f",
                  "kind": "dual_verdict",
                  "summary": "Returned unit_verdict PASS and verdict/user_level_verdict/upper_objective_verdict FAIL with agi_claim_supported=false."
                },
                {
                  "step": 7,
                  "component": "candidate_evaluate",
                  "kind": "post_result",
                  "summary": "The post-result boundary produced no activated Candidate and no provider or production route change."
                },
                {
                  "step": 8,
                  "component": "side_effect_state",
                  "kind": "frozen_request_boundary",
                  "summary": "The exact Consolidate request was frozen once and its response was absent at dispatch."
                }
              ],
              "success_condition_mapping": [
                {
                  "condition": "immutable precommit and no solution leakage",
                  "status": "PASS",
                  "evidence": "manifest/readiness ordering and immutable source identities"
                },
                {
                  "condition": "baseline must fail",
                  "status": "PASS",
                  "evidence": "sentinel True.intro rejected"
                },
                {
                  "condition": "candidate must compile",
                  "status": "PASS",
                  "evidence": "proof SHA-256 8a3efbe9789ed58172ba0c9cfc58ef508d2b67958e242591ff99c82a578a499f compiled"
                },
                {
                  "condition": "negative controls and clean replay",
                  "status": "PASS",
                  "evidence": "2/2 controls rejected and one clean replay passed"
                },
                {
                  "condition": "no upstream mutation",
                  "status": "PASS",
                  "evidence": "upstream_writes=0"
                },
                {
                  "condition": "original AGI/user objective",
                  "status": "FAIL",
                  "evidence": "independently_verified_agi_evidence=0 and no breadth, autonomy, or production evidence"
                }
              ],
              "evaluator_verdicts": {
                "unit_verdict": "PASS",
                "task_verdict": "FAIL",
                "user_level_verdict": "FAIL",
                "upper_objective_verdict": "FAIL",
                "agi_claim_supported": false
              },
              "candidate_decisions": {
                "pre_application": "BASELINE_FIRST",
                "post_result_activation": false,
                "global_activation": false,
                "production_routing_changed": false
              },
              "failures_and_repairs": [
                {
                  "failure": "Initial runner reached Context Kernel with a stale Work heartbeat after queue delay.",
                  "repair": "Durably refreshed only the existing generation-29 heartbeat under exact CAS and rechecked the unchanged request."
                },
                {
                  "failure": "First corrected runner used an oversized command argument.",
                  "repair": "Replaced it with JSON file input and validated the same expected-blob CAS path."
                },
                {
                  "failure": "Lifecycle validation expected Candidate pending although the engine had already frozen Consolidate.",
                  "repair": "Read the actual unique successor, changed only the assertion to consolidate_episode, and reran from response-absent authoritative main."
                }
              ],
              "side_effect_state": {
                "upstream_writes": 0,
                "candidate_activation": false,
                "provider_routing_changed": false,
                "production_routing_changed": false,
                "task_response_completed_exactly_once": true,
                "consolidate_request_recreated": false
              },
              "objective_evidence": [
                {
                  "kind": "immutable_source",
                  "finding": "The theorem source is commit ef9d2506ecf03bb3338e59d33edded195d94a9be and blob b1472e9b72df28045f33d0270f860ce793ffbd5e."
                },
                {
                  "kind": "bounded_verification",
                  "finding": "Candidate compilation=1, negative controls rejected=2/2, clean replay=1, upstream writes=0."
                },
                {
                  "kind": "protected_publication",
                  "finding": "PR416, PR417, and PR418 were merged only after exact-head required CI and remote readback."
                },
                {
                  "kind": "negative_evidence",
                  "finding": "No held-out breadth, cross-domain generalization, autonomous improvement, independent AGI evidence, or authorized activation exists."
                }
              ],
              "unresolved_end_goals": [
                "Consume Learn and return to Root.",
                "Select a materially different safe unit toward independent generalized capability evidence.",
                "Satisfy the strict external AGI evidence gate before any AGI or upper-objective PASS."
              ],
              "searchable_metadata": {
                "components": [
                  "root",
                  "candidate_evaluate",
                  "execute",
                  "task_evaluate",
                  "consolidate_episode"
                ],
                "domain": "Lean formal proof",
                "unit_verdict": "PASS",
                "upper_objective_verdict": "FAIL",
                "agi_claim_supported": false,
                "proof_sha256": "8a3efbe9789ed58172ba0c9cfc58ef508d2b67958e242591ff99c82a578a499f"
              }
            },
            "local_learn": {
              "decision": "NO_CHANGE",
              "candidates": [],
              "observations": [
                "Keep bounded unit PASS separate from original-task and AGI FAIL.",
                "Precommit ordering, negative controls, clean replay, exact-head CI, and remote readback are useful unit evidence but do not create breadth.",
                "The lifecycle recovery guard should continue validating the actual unique successor rather than assuming an intermediate component remains pending."
              ]
            },
            "fragment": {
              "component": "consolidate_episode",
              "unit_id": "unit-generation29-evaluate-external-formal-proof-synthesis-v1",
              "purpose": "Consolidate the exact external formal-proof synthesis, bounded dual verdict, non-activating Candidate decisions, recovery history, and unresolved upper objective without widening claims.",
              "verdict": "FAIL",
              "unit_verdict": "PASS",
              "agi_claim_supported": false,
              "candidate_activation": false,
              "evidence_refs": [
                ".continual/runs/run-work-recovery-gen9-durability-repair/artifacts/unit-generation29-external-formal-proof-synthesis-v1-result.json",
                ".continual/runs/run-work-recovery-gen9-durability-repair/artifacts/unit-generation29-evaluate-external-formal-proof-synthesis-v1-result.json",
                ".continual/work-model/invocations/invoke-a944e08fb0e821cea2feb29b/response.json"
              ],
              "unresolved": [
                "Learn and successor Root lifecycle",
                "materially different independent capability unit",
                "strict external AGI evidence and upper objective"
              ]
            }
          }
          TASKJSON
          PYTHONPATH=src python -m continual.cli --root . work-submit "$TASK_WORK_ID" --response /tmp/task-output.json --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/submitted.json
          PYTHONPATH=src python -m continual.cli --root . work-resume "$RUN_ID" --max-steps 2 --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/resumed.json
          PYTHONPATH=src python -m continual.cli --root . work-verify --run-id "$RUN_ID" > /tmp/verify-after.json
          PYTHONPATH=src python -m continual.cli --root . work-pending --run-id "$RUN_ID" > /tmp/pending-after.json
      - name: Validate and persist exact lifecycle
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json,os
          from pathlib import Path
          from continual.work_session import verified_work_invocation
          run_id=os.environ['RUN_ID'];wid=os.environ['TASK_WORK_ID'];nid=os.environ['TASK_NATIVE_ID']
          expected=json.load(open('/tmp/task-output.json'))
          verified=verified_work_invocation(Path('.').resolve(),wid)
          assert verified['output']==expected,verified
          native=json.loads(Path(f'.continual/runs/{run_id}/invocations/{nid}.json').read_text())
          assert native['status']=='complete' and native['component']=='consolidate_episode',native
          assert native['output']==expected,native
          before=json.load(open('/tmp/verify-before.json'));after=json.load(open('/tmp/verify-after.json'))
          assert after['valid'] is True and after['pending']==1,after
          for k in ('requests','responses','native_completed','native_fragments','native_local_learn'):
            assert after[k]==before[k]+1,(k,before,after)
          p=json.load(open('/tmp/pending-after.json'))['pending']
          assert len(p)==1 and p[0]['component']=='learn',p
          successor=p[0]
          assert successor['prompt_path']=='prompts/learn.md',successor
          assert successor['run_id']==run_id,successor
          assert successor['payload']['mode']=='post-task',successor
          assert successor['payload']['episode_id']==expected['result']['episode_id'],successor
          assert successor['payload']['current_episode']==expected['result'],successor
          waiting=[]
          for path in Path(f'.continual/runs/{run_id}/invocations').glob('*.json'):
            value=json.loads(path.read_text())
            if value.get('status')=='awaiting_work_model':waiting.append((path,value))
          assert len(waiting)==1 and waiting[0][1]['work_invocation_id']==successor['invocation_id'],waiting
          assert waiting[0][1]['component']=='learn',waiting
          snapshot=json.loads(Path(f'.continual/runs/{run_id}/snapshot.json').read_text())
          assert snapshot['revision']==140 and snapshot['phase']=='post_task_learn_pending' and snapshot['status']=='continue',snapshot
          assert snapshot['task_completion_verdict']=='FAIL' and snapshot['unit_completion_verdict']=='PASS',snapshot
          episode=json.loads(Path(f'.continual/episodes/{expected["result"]["episode_id"]}/episode.json').read_text())
          assert episode==expected['result'],episode
          print('CONSOLIDATE_CONSUMED_LEARN_FROZEN',verified['response']['response_digest'],successor['invocation_id'],successor['request_digest'],waiting[0][1]['invocation_id'])
          PY
          git checkout -- agi/WORK_EXECUTION_STATE.json
          test "$(git hash-object agi/WORK_EXECUTION_STATE.json)" = "$EXPECTED_STATE_BLOB"
          rm -f /tmp/pending-before.json /tmp/verify-before.json /tmp/submitted.json /tmp/resumed.json /tmp/verify-after.json /tmp/pending-after.json /tmp/task-output.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f .continual/work-model/invocations .continual/runs/$RUN_ID
          test -n "$(git status --porcelain)"
          git commit -m "[native-formal-proof-learn-frozen] consume Consolidate and freeze Learn"
          git push origin "HEAD:$GITHUB_REF_NAME"
