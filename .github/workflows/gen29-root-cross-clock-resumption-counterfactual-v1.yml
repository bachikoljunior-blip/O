# stop-cause-repair: bind CI-source observation to authoritative state exact_head_ci_observation
name: gen29 consume Root cross-clock resumption response v1

on:
  push:
    branches:
      - work/gen29-root-cross-clock-resumption-counterfactual-v1

permissions:
  contents: write

concurrency:
  group: gen29-root-cross-clock-resumption-counterfactual-v1
  cancel-in-progress: false

jobs:
  consume:
    if: ${{ !contains(github.event.head_commit.message, '[native-root-result]') }}
    runs-on: ubuntu-latest
    timeout-minutes: 25
    env:
      GH_TOKEN: ${{ github.token }}
      EXPECTED_MAIN: ec463c4a0f79e636d4c37d877530f849458ef028
      EXPECTED_STATE_BLOB: 8ee86341f54b89748d0db1a3accb1b9ea22230ad
      EXPECTED_SNAPSHOT_BLOB: adff39ef562a809123becd81d52c9f4088c470b1
      EXPECTED_INBOX_BLOB: 34423c2dd71d95c55769f501fe1701ea0088fb32
      EXPECTED_INBOX_REVISION: '38'
      ROOT_NATIVE_ID: invoke-669af2b4afa417cbe8f3e0d5
      ROOT_NATIVE_BLOB: a01653cd6b37b5da12d7a0e6317839c8b72de23d
      ROOT_WORK_ID: invoke-cb5b97a104622aea5f019d38
      ROOT_REQUEST_BLOB: 4a6d7856d0619979a810bc562bf0a46f06c2316f
      ROOT_REQUEST_DIGEST: eefcf711a7eb32f389d5e91dea9a7c39d5bb6824887a41842e166851002ab3a8
      SOURCE_CI_RUN_ID: '33384147662'
      SOURCE_CI_HEAD: 93578c7973fdf5170df09931b0a5dd8448791d00
      RUN_ID: run-work-recovery-gen9-durability-repair
      RESULT_UNIT_ID: unit-generation29-cross-clock-resumption-counterfactual-v1
      EXECUTOR_BINDING: current_chatgpt_work_session
      MODEL_IDENTITY: chatgpt-work-model-unverified
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: pip
      - name: Install exact repository runtime
        run: python -m pip install -e .

      - name: Assert exact branch, runtime main, authority, and Root binding
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main --no-tags
          RUNTIME_MAIN="$(git rev-parse origin/main)"
          test "$RUNTIME_MAIN" = "$EXPECTED_MAIN"
          test "$(git merge-base "$EXPECTED_MAIN" HEAD)" = "$EXPECTED_MAIN"
          python - <<'PY'
          import os
          import subprocess
          expected = ['.github/workflows/gen29-root-cross-clock-resumption-counterfactual-v1.yml']
          observed = subprocess.check_output(
              ['git', 'diff', '--name-only', os.environ['EXPECTED_MAIN'], 'HEAD'],
              text=True,
          ).splitlines()
          if observed != expected:
              raise SystemExit(f'launch delta mismatch: {observed!r}')
          PY
          gh api "repos/$GITHUB_REPOSITORY/contents/agi/WORK_EXECUTION_STATE.json?ref=$RUNTIME_MAIN" > /tmp/runtime-state-document.json
          gh api "repos/$GITHUB_REPOSITORY/contents/agi/USER_INPUT_INBOX.json?ref=$RUNTIME_MAIN" > /tmp/runtime-inbox-document.json
          RUNTIME_STATE_BLOB="$(jq -r '.sha' /tmp/runtime-state-document.json)"
          test "$RUNTIME_STATE_BLOB" = "$EXPECTED_STATE_BLOB"
          test "$(jq -r '.sha' /tmp/runtime-inbox-document.json)" = "$EXPECTED_INBOX_BLOB"
          jq -r '.content' /tmp/runtime-state-document.json | tr -d '\n' | base64 --decode > agi/WORK_EXECUTION_STATE.json
          jq -r '.content' /tmp/runtime-inbox-document.json | tr -d '\n' | base64 --decode > /tmp/runtime-inbox.json
          test "$(git hash-object agi/WORK_EXECUTION_STATE.json)" = "$EXPECTED_STATE_BLOB"
          test "$(git hash-object /tmp/runtime-inbox.json)" = "$EXPECTED_INBOX_BLOB"
          test "$(git hash-object .continual/runs/$RUN_ID/snapshot.json)" = "$EXPECTED_SNAPSHOT_BLOB"
          test "$(git hash-object .continual/work-model/invocations/$ROOT_WORK_ID/request.json)" = "$ROOT_REQUEST_BLOB"
          test "$(git hash-object .continual/runs/$RUN_ID/invocations/$ROOT_NATIVE_ID.json)" = "$ROOT_NATIVE_BLOB"
          echo "RUNTIME_MAIN=$RUNTIME_MAIN" >> "$GITHUB_ENV"
          echo "RUNTIME_STATE_BLOB=$RUNTIME_STATE_BLOB" >> "$GITHUB_ENV"
          python - <<'PY'
          import hashlib
          import json
          import os
          from pathlib import Path
          from continual.work_session import pending_work_invocations, verified_work_request, verify_work_invocations
          root = Path('.').resolve()
          run_id = os.environ['RUN_ID']
          native_id = os.environ['ROOT_NATIVE_ID']
          work_id = os.environ['ROOT_WORK_ID']
          state = json.loads((root / 'agi/WORK_EXECUTION_STATE.json').read_text())
          inbox = json.loads(Path('/tmp/runtime-inbox.json').read_text())
          snapshot = json.loads((root / '.continual' / 'runs' / run_id / 'snapshot.json').read_text())
          request = verified_work_request(root, work_id)
          native_path = root / '.continual' / 'runs' / run_id / 'invocations' / f'{native_id}.json'
          native = json.loads(native_path.read_text())
          if state.get('execution_id') != 'work-recovery-20260828T122724304Z-b9f52bccaba0472923908d65b1fb59c3':
              raise SystemExit('execution mismatch')
          if state.get('lease_generation') != 29 or hashlib.sha256(state['fence_token'].encode()).hexdigest() != 'c03cdd410193ed54352ee4fd5480cf5e4fccb77d5a1aca67809b7c3e4341890d':
              raise SystemExit('generation/fence mismatch')
          if state.get('status') != 'running' or state.get('resume_required') is not True:
              raise SystemExit('authority is not resumable running state')
          if state.get('active_component') != 'generation29_root_post_learn_no_change_pending_native_consume':
              raise SystemExit('active component mismatch')
          exact = state.get('exact_continuation', {})
          expected = {
              'completed_native_invocation_id': 'invoke-2bc2261e87d1bd5f27aa083c',
              'completed_work_invocation_id': 'invoke-c5c119aff74ae340fd143b9b',
              'completed_response_digest': 'ff47eb9fded9490fd2d6ea501c236400413096d730d75529a40a7a02e44cb44e',
              'pending_native_invocation_id': native_id,
              'pending_native_invocation_ref': f'.continual/runs/{run_id}/invocations/{native_id}.json',
              'pending_native_invocation_blob_sha': os.environ['ROOT_NATIVE_BLOB'],
              'pending_work_invocation_id': work_id,
              'pending_request_ref': f'.continual/work-model/invocations/{work_id}/request.json',
              'pending_request_blob_sha': os.environ['ROOT_REQUEST_BLOB'],
              'pending_request_digest': os.environ['ROOT_REQUEST_DIGEST'],
              'pending_response_ref': None,
              'pending_response_blob_sha': None,
              'pending_response_digest': None,
              'response_exists_on_authoritative_remote': False,
              'snapshot_revision': 118,
              'native_phase': 'root_pending',
              'pending_component': 'root',
              'durability_status': 'pending_native_consume',
          }
          for key, value in expected.items():
              if exact.get(key) != value:
                  raise SystemExit(f'exact continuation mismatch {key}: {exact.get(key)!r} != {value!r}')
          bind = state.get('generation29_learn_postcommit_clean_g1_not_evaluable_root_bind_v1', {})
          if bind.get('status') != 'learn_completed_root_unique_pending_remote_main_readback_verified':
              raise SystemExit('Root state binding status mismatch')
          durability = state.get('continuation_durability', {})
          if durability.get('status') != 'remote_main_readback_verified' or durability.get('verified_remote_readback') is not True:
              raise SystemExit('continuation durability is not verified')
          if durability.get('pending_work_invocation_id') != work_id or durability.get('pending_native_invocation_id') != native_id:
              raise SystemExit('continuation durability identity mismatch')
          if inbox.get('revision') != 38:
              raise SystemExit('inbox revision mismatch')
          if snapshot.get('revision') != 118 or snapshot.get('phase') != 'root_pending' or snapshot.get('status') != 'continue':
              raise SystemExit('snapshot is not revision118 root_pending')
          if request.get('component') != 'root' or request.get('request_digest') != os.environ['ROOT_REQUEST_DIGEST']:
              raise SystemExit('Root request mismatch')
          if request.get('prompt_path') != 'prompts/root.md':
              raise SystemExit('Root prompt mismatch')
          if request.get('executor_binding') != os.environ['EXECUTOR_BINDING'] or request.get('model_identity') != os.environ['MODEL_IDENTITY']:
              raise SystemExit('Root identity mismatch')
          if (root / '.continual' / 'work-model' / 'invocations' / work_id / 'response.json').exists():
              raise SystemExit('Root already answered')
          if native.get('status') != 'awaiting_work_model' or native.get('component') != 'root':
              raise SystemExit('Root native is not awaiting')
          if native.get('work_invocation_id') != work_id or native.get('work_request_digest') != request.get('request_digest'):
              raise SystemExit('native/Work binding mismatch')
          pending = pending_work_invocations(root, run_id=run_id)
          if len(pending) != 1 or pending[0].get('invocation_id') != work_id:
              raise SystemExit('Root is not the unique pending Work item')
          verified = verify_work_invocations(root, run_id=run_id)
          expected_counts = {'valid': True, 'requests': 122, 'responses': 121, 'pending': 1, 'native_completed': 115, 'native_fragments': 115, 'native_local_learn': 105}
          for key, value in expected_counts.items():
              if verified.get(key) != value:
                  raise SystemExit(f'before verification mismatch {key}: {verified.get(key)!r}')
          Path('/tmp/expected-before.json').write_text(json.dumps(expected_counts, sort_keys=True) + '\n')
          PY
          continual --root . work-checkpoint-verify --state agi/WORK_EXECUTION_STATE.json > /tmp/work-checkpoint-before.json

      - name: Record fresh exact main and exact-head CI observations
        shell: bash
        run: |
          set -euo pipefail
          continual --root . work-source-observation-prepare "$RUN_ID" --state-blob-sha "$RUNTIME_STATE_BLOB" --expected-commit-sha "$RUNTIME_MAIN" --model-identity "$MODEL_IDENTITY" > /tmp/work-source-prepare.json
          python - <<'PY'
          import base64
          import json
          from pathlib import Path
          from continual.store import Store
          document = json.loads(Path('/tmp/runtime-state-document.json').read_text())
          state = json.loads(base64.b64decode(''.join(document['content'].splitlines())).decode())
          projection = {
              'status': state['status'], 'owner_kind': state['owner_kind'],
              'execution_id': state['execution_id'], 'lease_generation': state['lease_generation'],
              'fence_token_digest': Store(Path('.')).stable_digest(state['fence_token'], length=64),
              'heartbeat_at': state['heartbeat_at'],
          }
          Path('/tmp/work-source-projection.json').write_text(json.dumps(projection, sort_keys=True) + '\n')
          PY
          WORK_OBSERVATION_ID="$(jq -r '.observation_id' /tmp/work-source-prepare.json)"
          WORK_REQUEST_DIGEST="$(jq -r '.request_digest' /tmp/work-source-prepare.json)"
          OBSERVED_AT="$(python -c 'from datetime import UTC, datetime; print(datetime.now(UTC).isoformat().replace("+00:00", "Z"))')"
          continual --root . work-source-observation-record "$RUN_ID" "$WORK_OBSERVATION_ID" --request-digest "$WORK_REQUEST_DIGEST" --commit-sha "$RUNTIME_MAIN" --blob-sha "$RUNTIME_STATE_BLOB" --projection /tmp/work-source-projection.json --observed-at "$OBSERVED_AT" --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/work-source-receipt.json
          continual --root . ci-source-observation-prepare "$RUN_ID" --model-identity "$MODEL_IDENTITY" > /tmp/ci-source-prepare.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$SOURCE_CI_RUN_ID" > /tmp/workflow-run-full.json
          gh api "repos/$GITHUB_REPOSITORY/actions/runs/$SOURCE_CI_RUN_ID/jobs?per_page=100" > /tmp/workflow-jobs-full.json
          test "$(jq -r '.head_sha' /tmp/workflow-run-full.json)" = "$SOURCE_CI_HEAD"
          test "$(jq -r '.status' /tmp/workflow-run-full.json)" = "completed"
          test "$(jq -r '.conclusion' /tmp/workflow-run-full.json)" = "success"
          jq -e '[.jobs[] | select(.name == "pytest shard 0 of 4" or .name == "pytest shard 1 of 4" or .name == "pytest shard 2 of 4" or .name == "pytest shard 3 of 4" or .name == "test") | select(.status == "completed" and .conclusion == "success")] | length == 5' /tmp/workflow-jobs-full.json
          jq '{id, workflow_id, name, status, conclusion, head_sha}' /tmp/workflow-run-full.json > /tmp/workflow-run-projection.json
          jq '[.jobs[] | {id, name, status, conclusion}] | sort_by(.id)' /tmp/workflow-jobs-full.json > /tmp/workflow-jobs-projection.json
          CI_OBSERVATION_ID="$(jq -r '.observation_id' /tmp/ci-source-prepare.json)"
          CI_REQUEST_DIGEST="$(jq -r '.request_digest' /tmp/ci-source-prepare.json)"
          OBSERVED_AT="$(python -c 'from datetime import UTC, datetime; print(datetime.now(UTC).isoformat().replace("+00:00", "Z"))')"
          continual --root . ci-source-observation-record "$RUN_ID" "$CI_OBSERVATION_ID" --request-digest "$CI_REQUEST_DIGEST" --workflow-run /tmp/workflow-run-projection.json --jobs /tmp/workflow-jobs-projection.json --observed-at "$OBSERVED_AT" --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/ci-source-receipt.json

      - name: Recheck CAS and continuity before mutation
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin main --no-tags
          LATEST_MAIN="$(git rev-parse origin/main)"
          test "$LATEST_MAIN" = "$EXPECTED_MAIN"
          gh api "repos/$GITHUB_REPOSITORY/contents/agi/WORK_EXECUTION_STATE.json?ref=$LATEST_MAIN" > /tmp/pre-mutation-state-document.json
          test "$(jq -r '.sha' /tmp/pre-mutation-state-document.json)" = "$RUNTIME_STATE_BLOB"
          python - <<'PY'
          import json
          import os
          from pathlib import Path
          from continual.ci_source_observation import verify_ci_source_observation
          from continual.continuity_preflight import assert_work_resume_continuity_preflight
          from continual.store import Store
          from continual.work_source_observation import verify_work_source_observation
          root = Path('.').resolve()
          state = json.loads((root / 'agi/WORK_EXECUTION_STATE.json').read_text())
          now = Store(root).utc_now()
          work = verify_work_source_observation(root, run_id=os.environ['RUN_ID'], state=state, state_blob_sha=os.environ['RUNTIME_STATE_BLOB'], now=now)
          ci = verify_ci_source_observation(root, run_id=os.environ['RUN_ID'], state=state, now=now)
          continuity = assert_work_resume_continuity_preflight(root, run_id=os.environ['RUN_ID'], executor_binding=os.environ['EXECUTOR_BINDING'], model_identity=os.environ['MODEL_IDENTITY'])
          if continuity.get('resume_authorized') is not True:
              raise SystemExit('continuity preflight denied')
          print(json.dumps({'work': work, 'ci': ci, 'continuity': continuity}, sort_keys=True))
          PY

      - name: Submit exact Root response and freeze successor preflight
        shell: bash
        run: |
          set -euo pipefail
          cat > /tmp/root-output.json <<'JSON'
          {
            "result": {
              "component": "execute",
              "unit_id": "unit-generation29-cross-clock-resumption-counterfactual-v1",
              "goal": "Run one bounded protected-versus-fail-open resumption benchmark across three isolated interruptions: stale Work heartbeat before successor freeze, moved authoritative state/main binding, and a newer inbox revision at dispatch. Prove that protected O rejects stale authority, preserves the exact request, repairs the concrete cause, and resumes exactly once without stale-writer or duplicate effects.",
              "scope": "Synthetic repository checkpoint copies only; no live generation-29 authority mutation inside the benchmark, no repeat of the unavailable forecast, no Candidate/routing change, and no AGI or completion claim.",
              "inputs": [
                "revision118 frozen Root context plus verified stale-heartbeat failure/repair runs 33382998357 and 33383852840",
                "Context Kernel freshness, immutable Work journal, CAS/fence, inbox binding, continuation verifier, and idempotency machinery",
                "synthetic stale-heartbeat, moved-state-blob, and newer-inbox fixtures",
                "an isolated fail-open control disabling only the scenario guard"
              ],
              "evidence_needed": [
                "a precommitted manifest binding scenarios/configurations/expected outcomes/digests and forbidding live-authority writes",
                "deterministic protected and control traces through attempted dispatch, rejection or failure, repair, exact request preservation, resume, and successor freeze",
                "counters for submissions, completions, successors, stale commits, duplicates, fence changes, and inbox bypasses",
                "focused and protected exact-head CI, expected-head merge, and readback"
              ],
              "expected_outputs": [
                "a versioned three-scenario/two-configuration benchmark and synthetic fixture builders",
                "deterministic per-scenario traces, counters, rejection/repair reasons, and replay digests",
                "an aggregate mapping each guard to the concrete stale or duplicate behavior it prevents",
                "focused tests proving byte-stable replay, unique successor after repair, no stale writer, no duplicate response, and no live-authority mutation"
              ],
              "success_conditions": [
                "stale-heartbeat protection fails before successor freeze and heartbeat-only repair yields exactly one response and successor",
                "moved-state protection rejects stale CAS/fence while fail-open exhibits the precommitted stale-write or divergent-successor outcome",
                "newer-inbox protection delays dispatch until safe-boundary ingestion while fail-open exhibits the stale-context action",
                "all protected traces have zero duplicates or unauthorized authority changes, deterministic replay, protected CI, expected-head merge, and exact readback"
              ],
              "failure_conditions": [
                "any fixture writes live authority or consumes the frozen Root/successor outside this exact workflow",
                "protected execution accepts stale heartbeat/blob/fence/inbox and emits a successor or effect",
                "repair recreates identity, duplicates an effect, or yields multiple successors",
                "the control is non-informative, the prior forecast is rerun/upgraded, claims broaden, or validation/publication controls fail"
              ],
              "parent_continuation": "After Execute, Task Evaluate only these three protected-versus-control traces, exactly-once counters, and live-authority non-mutation proof. Then Consolidate Episode and Learn, publish through exact-head CI and expected-head merge with readback, return to Root, and choose a materially different safe unit while AGI and user completion remain false."
            },
            "local_learn": {
              "decision": "NO_CHANGE",
              "candidates": [],
              "observation": "A real cross-clock stop and successful repair justify a falsifiable behavioral benchmark, not another forecast retry or a capability claim."
            },
            "fragment": {
              "component": "root",
              "observations": [
                "The forecast remains NOT_EVALUABLE with zero outcome effects.",
                "Learn was NO_CHANGE and Candidate/routing/AGI/completion remain false.",
                "Protected-versus-control cross-clock resumption directly probes autonomous continuity with safe synthetic fixtures."
              ]
            }
          }
          JSON
          python -m json.tool /tmp/root-output.json > /dev/null
          # Response bytes are frozen by this exact-head one-shot workflow and compared after submission.
          continual --root . work-submit "$ROOT_WORK_ID" --response /tmp/root-output.json --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/root-submit.json
          continual --root . work-resume "$RUN_ID" --max-steps 2 --executor-binding "$EXECUTOR_BINDING" --model-identity "$MODEL_IDENTITY" > /tmp/native-resume.json
          continual --root . work-verify --run-id "$RUN_ID" > /tmp/work-verify-after.json
          continual --root . work-pending --run-id "$RUN_ID" > /tmp/work-pending-after.json

      - name: Validate exact Root completion and Candidate Evaluate freeze
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import os
          import subprocess
          from datetime import UTC, datetime
          from pathlib import Path
          from continual.work_session import pending_work_invocations, verified_work_invocation, verify_work_invocations
          root = Path('.').resolve()
          run_id = os.environ['RUN_ID']
          root_native_id = os.environ['ROOT_NATIVE_ID']
          root_work_id = os.environ['ROOT_WORK_ID']
          unit_id = os.environ['RESULT_UNIT_ID']
          expected_output = json.loads(Path('/tmp/root-output.json').read_text())
          after = verify_work_invocations(root, run_id=run_id)
          expected_after = {'valid': True, 'requests': 123, 'responses': 122, 'pending': 1, 'native_completed': 116, 'native_fragments': 116, 'native_local_learn': 106}
          for key, value in expected_after.items():
              if after.get(key) != value:
                  raise SystemExit(f'after mismatch {key}: {after.get(key)!r} != {value!r}')
          verified_root = verified_work_invocation(root, root_work_id)
          if verified_root.get('output') != expected_output:
              raise SystemExit('persisted Root output mismatch')
          root_native_path = root / '.continual' / 'runs' / run_id / 'invocations' / f'{root_native_id}.json'
          root_native = json.loads(root_native_path.read_text())
          if root_native.get('status') != 'complete' or root_native.get('component') != 'root' or root_native.get('output') != expected_output:
              raise SystemExit('Root native completion mismatch')
          response_rel = f'.continual/work-model/invocations/{root_work_id}/response.json'
          fragment_rel = root_native.get('fragment_ref')
          local_learn_rel = f'.continual/runs/{run_id}/local-learn/{root_native_id}-root.json'
          for rel in (response_rel, fragment_rel, local_learn_rel):
              if not rel or not (root / rel).is_file():
                  raise SystemExit(f'Root lifecycle path missing: {rel}')
          unit_path = root / '.continual' / 'runs' / run_id / 'execution-units' / f'{unit_id}.json'
          if json.loads(unit_path.read_text()) != expected_output['result']:
              raise SystemExit('persisted execution unit mismatch')
          snapshot_path = root / '.continual' / 'runs' / run_id / 'snapshot.json'
          snapshot = json.loads(snapshot_path.read_text())
          expected_snapshot = {'revision': 119, 'phase': 'unit_pending', 'status': 'continue', 'current_component': 'execute', 'current_unit': unit_id}
          for key, value in expected_snapshot.items():
              if snapshot.get(key) != value:
                  raise SystemExit(f'snapshot mismatch {key}: {snapshot.get(key)!r} != {value!r}')
          pending = pending_work_invocations(root, run_id=run_id)
          if len(pending) != 1:
              raise SystemExit(f'expected one successor pending request, got {len(pending)}')
          request = pending[0]
          if request.get('component') != 'candidate_evaluate':
              raise SystemExit(f'expected Candidate Evaluate successor, got {request.get("component")!r}')
          payload = request.get('payload', {})
          if payload.get('mode') != 'pre-application' or payload.get('target_component') != 'execute':
              raise SystemExit('Candidate Evaluate request is not the scoped Execute preflight')
          execution_context = payload.get('execution_unit', {})
          if execution_context.get('execution_unit') != expected_output['result']:
              raise SystemExit('Candidate Evaluate request did not bind exact Root-selected unit')
          frozen_snapshot = execution_context.get('snapshot', {})
          if frozen_snapshot.get('revision') != 109 or frozen_snapshot.get('phase') != 'unit_pending' or frozen_snapshot.get('current_component') != 'execute' or frozen_snapshot.get('current_unit') != unit_id:
              raise SystemExit('Candidate Evaluate request snapshot binding mismatch')
          candidates = payload.get('candidate_index', {}).get('candidates', [])
          if not isinstance(candidates, list):
              raise SystemExit('Candidate Evaluate candidate index is not frozen as a list')
          work_id = request['invocation_id']
          request_rel = f'.continual/work-model/invocations/{work_id}/request.json'
          if (root / request_rel).with_name('response.json').exists():
              raise SystemExit('Candidate Evaluate request already has a response')
          awaiting = []
          for path in sorted((root / '.continual' / 'runs' / run_id / 'invocations').glob('*.json')):
              value = json.loads(path.read_text())
              if value.get('status') == 'awaiting_work_model':
                  awaiting.append((path, value))
          if len(awaiting) != 1:
              raise SystemExit(f'expected one awaiting native journal, got {len(awaiting)}')
          native_path, native = awaiting[0]
          if native.get('component') != 'candidate_evaluate' or native.get('work_invocation_id') != work_id:
              raise SystemExit('successor native-to-Work identity mismatch')
          if native.get('work_request_digest') != request.get('request_digest') or native.get('work_request_ref') != request_rel:
              raise SystemExit('successor native-to-Work digest/ref mismatch')
          summary = {
              'root_native_invocation_id': root_native_id,
              'root_work_invocation_id': root_work_id,
              'root_response_ref': response_rel,
              'root_response_digest': verified_root['response']['response_digest'],
              'root_response_blob_sha': subprocess.check_output(['git','hash-object',response_rel], text=True).strip(),
              'root_native_blob_sha': subprocess.check_output(['git','hash-object',str(root_native_path)], text=True).strip(),
              'root_fragment_ref': fragment_rel,
              'root_fragment_blob_sha': subprocess.check_output(['git','hash-object',fragment_rel], text=True).strip(),
              'root_local_learn_ref': local_learn_rel,
              'root_local_learn_blob_sha': subprocess.check_output(['git','hash-object',local_learn_rel], text=True).strip(),
              'execution_unit_ref': unit_path.relative_to(root).as_posix(),
              'execution_unit_blob_sha': subprocess.check_output(['git','hash-object',str(unit_path)], text=True).strip(),
              'pending_native_invocation_id': native['invocation_id'],
              'pending_native_invocation_ref': native_path.relative_to(root).as_posix(),
              'pending_native_invocation_blob_sha': subprocess.check_output(['git','hash-object',str(native_path)], text=True).strip(),
              'pending_work_invocation_id': work_id,
              'pending_request_ref': request_rel,
              'pending_request_digest': request['request_digest'],
              'pending_request_blob_sha': subprocess.check_output(['git','hash-object',request_rel], text=True).strip(),
              'snapshot_revision': 119,
              'snapshot_phase': 'unit_pending',
              'snapshot_blob_sha': subprocess.check_output(['git','hash-object',str(snapshot_path)], text=True).strip(),
              'verification': expected_after,
              'selected_semantic_component': 'execute',
              'pending_preflight_component': 'candidate_evaluate',
          }
          Path('/tmp/root-consume-summary.json').write_text(json.dumps(summary, indent=2, sort_keys=True) + '\n')
          receipt_rel = f'.continual/runs/{run_id}/recovery-receipts/gen29-root-consume-cross-clock-resumption-freeze-v1.json'
          receipt = {
              'schema_version': 1,
              'record_type': 'generation29_root_response_and_candidate_preflight_receipt',
              'recorded_at': datetime.now(UTC).isoformat().replace('+00:00','Z'),
              'authority': {'execution_id':'work-recovery-20260828T122724304Z-b9f52bccaba0472923908d65b1fb59c3','lease_generation':29,'fence_token_digest':'c03cdd410193ed54352ee4fd5480cf5e4fccb77d5a1aca67809b7c3e4341890d'},
              'source_main_sha': os.environ['RUNTIME_MAIN'],
              'source_state_blob_sha': os.environ['RUNTIME_STATE_BLOB'],
              'source_ci_workflow_run_id': int(os.environ['SOURCE_CI_RUN_ID']),
              'idempotency_key': 'o-work-gen29:consume-root-invoke-cb5b97a104622aea5f019d38:freeze-cross-clock-resumption-counterfactual-preflight:v1',
              'completed_effect': summary,
              'request_recreated': False,
              'root_reinvoked': False,
              'resume_required': True,
              'next_action': 'Exact-head validate and merge this Root response and Candidate Evaluate preflight freeze, bind state, answer the Candidate Evaluate request exactly once, then execute the bounded blind-holder protocol repair unit.',
          }
          receipt_path = root / receipt_rel
          receipt_path.parent.mkdir(parents=True, exist_ok=True)
          receipt_path.write_text(json.dumps(receipt, indent=2, sort_keys=True) + '\n')
          PY

      - name: Stage only exact lifecycle delta and push once
        shell: bash
        run: |
          set -euo pipefail
          git checkout -- agi/WORK_EXECUTION_STATE.json
          test "$(git hash-object agi/WORK_EXECUTION_STATE.json)" = "$EXPECTED_STATE_BLOB"
          test "$(git hash-object .continual/work-model/invocations/$ROOT_WORK_ID/request.json)" = "$ROOT_REQUEST_BLOB"
          python - <<'PY'
          import json
          import os
          import subprocess
          from pathlib import Path
          run_id = os.environ['RUN_ID']
          root_native_id = os.environ['ROOT_NATIVE_ID']
          root_work_id = os.environ['ROOT_WORK_ID']
          summary = json.loads(Path('/tmp/root-consume-summary.json').read_text())
          work_obs = json.loads(Path('/tmp/work-source-prepare.json').read_text())['observation_id']
          ci_obs = json.loads(Path('/tmp/ci-source-prepare.json').read_text())['observation_id']
          receipt_rel = f'.continual/runs/{run_id}/recovery-receipts/gen29-root-consume-cross-clock-resumption-freeze-v1.json'
          expected_status = {
              f'.continual/runs/{run_id}/events.jsonl': 'M',
              f'.continual/runs/{run_id}/snapshot.json': 'M',
              f'.continual/runs/{run_id}/invocations/{root_native_id}.json': 'M',
              f'.continual/work-model/invocations/{root_work_id}/response.json': 'A',
              summary['root_fragment_ref']: 'A',
              summary['root_local_learn_ref']: 'A',
              summary['execution_unit_ref']: 'A',
              summary['pending_native_invocation_ref']: 'A',
              summary['pending_request_ref']: 'A',
              f'.continual/runs/{run_id}/work-source-observations/{work_obs}/request.json': 'A',
              f'.continual/runs/{run_id}/work-source-observations/{work_obs}/receipt.json': 'A',
              f'.continual/runs/{run_id}/ci-source-observations/{ci_obs}/request.json': 'A',
              f'.continual/runs/{run_id}/ci-source-observations/{ci_obs}/receipt.json': 'A',
              receipt_rel: 'A',
          }
          status = {}
          for line in subprocess.check_output(['git','diff','--name-status'], text=True).splitlines():
              code, path = line.split('\t',1)
              status[path] = code
          for path in subprocess.check_output(['git','ls-files','--others','--exclude-standard'], text=True).splitlines() + subprocess.check_output(['git','ls-files','--others','--ignored','--exclude-standard'], text=True).splitlines():
              status.setdefault(path,'A')
          for path in list(status):
              if path.startswith('src/agi/__pycache__/') or path.startswith('src/continual/__pycache__/') or path.startswith('src/continual_chatgpt.egg-info/'):
                  status.pop(path,None)
          preflight_prefix = f'.continual/runs/{run_id}/preflight/preflight-'
          preflight_paths = sorted(path for path, code in status.items() if path.startswith(preflight_prefix) and path.endswith('.json') and code == 'A')
          for path in preflight_paths:
              expected_status[path] = 'A'
          if status != expected_status:
              raise SystemExit('lifecycle mutation mismatch: ' + json.dumps({'expected':expected_status,'observed':status,'missing':sorted(set(expected_status)-set(status)),'unexpected':sorted(set(status)-set(expected_status))}, sort_keys=True))
          required = sorted(expected_status)
          for path in required:
              if not Path(path).is_file():
                  raise SystemExit('required lifecycle path missing: ' + path)
          subprocess.run(['git','add','-f','--',*required], check=True)
          staged = set(subprocess.check_output(['git','diff','--cached','--name-only'], text=True).splitlines())
          if staged != set(required):
              raise SystemExit('staged lifecycle set mismatch')
          immutable = sorted(path for path in staged if path.startswith('.continual/work-model/invocations/'))
          expected_immutable = sorted([f'.continual/work-model/invocations/{root_work_id}/response.json', summary['pending_request_ref']])
          if immutable != expected_immutable:
              raise SystemExit('immutable Work mutation set mismatch: ' + json.dumps(immutable))
          PY
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m '[native-root-result] gen29 consume Root and freeze cross-clock resumption preflight'
          git push origin "HEAD:$GITHUB_REF_NAME"
